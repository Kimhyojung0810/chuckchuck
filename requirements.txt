# 파이썬 패키지 의존성 목록입니다. pip install -r requirements.txt 로 설치합니다.

# 코어 모듈 (chuckchuck/)
requests>=2.31.0

# 서버 (server/) — 프론트가 실제로 붙는 런타임이다. 빠지면 `python -m server` 가
# ModuleNotFoundError 로 죽고, tests/test_flat_routes.py 가 수집 단계에서 깨져
# `python -m pytest tests/ -q` 전체가 실패한다.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9   # 업로드 라우트(UploadFile) 가 요구한다

# 테스트 — README 가 설치 직후 `python -m pytest tests/ -q` 를 지시한다
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27.0             # fastapi.testclient 가 요구한다
