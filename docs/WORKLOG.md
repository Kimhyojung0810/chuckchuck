<!-- 이 파일: 날짜별 작업 일지입니다. 세션(사람·AI)마다 무슨 작업을 왜 했는지 남깁니다.

작성 규칙
- 최신 날짜가 위로 온다. 같은 날 여러 세션이면 `## 날짜 — 제목 (작업자)` 절을 나눠 쓴다.
- 한 덩어리(커밋 단위)마다: 커밋 해시 · 무엇을 · 왜 · 어떻게 검증했는지.
- "왜"가 핵심이다 — 무엇을 고쳤는지는 diff 가 말한다 (CLAUDE.md §3-5 와 같은 규율).
- 완결된 대형 감사 기록은 별도 문서로 두고 여기서 링크한다 (예: [QA_FIXES.md](QA_FIXES.md)).
-->

# 작업 일지

## 2026-09-13 (낮) — 팀 전체 공유문 + 내용 제안 1단 (Claude 세션, 사용자 요청)

- **비개발 팀원용 공유문** `docs/회의록/공유_2026-09-13_팀_전체.md` + 같은 내용의 웹 페이지
  (https://claude.ai/code/artifact/3a01e494-a2bc-4ed4-ba4c-b068f31f958d, 비공개·팀 공유 가능). 개발 용어 없이
  「바뀐 것 전·후 / 막힌 것과 누가 풀 수 있나 / 오늘 부탁 세 가지(녹음 1건·채점 절반·디자인 담당) / 일정 / 부스에서 눌러 볼 것 / 용어 한 줄」.
  다음 주간 공유는 같은 URL 을 갱신한다 (`docs/회의록/README.md` 표).
- **내용 제안 1단** (`712cec3`·`ba4dbfa`, 로드맵 B6 서버 쪽): `f23_context.deck_gaps` · `POST /api/v1/deck-gaps`. 상황별 「청중이
  기대하는 것」(업무 보고: 현황·이슈·다음 행동·결정 요청·리스크 …) 을 낱말 신호로 있음/약함/없음 + 근거 장. 상황이 비면 F-23 추정값.
  호출 0. 화면은 Festa 뒤(missing 먼저). LLM 2단은 표본이 오면. SCHEMA §10-D.
- 실수 기록: 테스트 실패가 `| tail` 파이프에 가려진 채 두 번 커밋됐다 (`0fd6bb7` 뒤, `712cec3`). 둘 다 바로 고쳤지만 이후 셸은 `set -o pipefail`.

## 2026-09-13 — `#/new` 상황 미리 채우기 + 질문 코칭 음성 입력 점검 (Claude 세션, 사용자 요청)

### 1. `#/new` 폼 미리 채우기 (로드맵 A6, `d35a352`)

- 업로드 직후 `/api/v1/suggest-context` → `nf.suggest`. 2단계 폼이 **비어 있을 때만** 상황 칩을 골라 두고,
  「자료를 보니 ‘팀에 보고해요’ 같아서 골라 뒀어요 · 1장 '실적' · 3장 '요청 사항'. 아니면 다른 걸 눌러 주세요.」 한 줄.
- 칩을 한 번 누르면 `occTouched` — 추정은 물러난다. 건너뛰기는 그대로 상황을 비운다. 새 화면·바텀시트 없음(§3-1 다크패턴 금지).
- SDK `suggestContext()` 는 실패해도 null (업로드 흐름을 막지 않는다). app.js ql2→ql3 · chuckchuck_bridge.js qkw→qkx · node smoke 33.

### 2. 질문 코칭 음성 입력 — 점검 결과

경로: 마이크 → (Chrome 실시간 받아쓰기 | MediaRecorder 녹음 90초 상한) → `/api/v1/transcribe` (marks=[]) → `full_text` 를 입력창에 채움 → **사용자가 보내기**.

| 확인한 것 | 결과 |
|---|---|
| 실 A.X STT 로 저장 녹음(m4a 11MB, 5분 47초) 을 답변 모양(marks=[])으로 | HTTP 200 · 6.6초 · full_text 2,128자 · words 547. **경로는 살아 있다** |
| 10 MiB 미만 업로드의 WAF 거부 (8/7 실측 0.5~8MB 전부 REJECT) | **오늘은 재현 안 됨** — 93KB·937KB WAV 업로드 통과(0.2~0.4초). 8/7 의 User-Agent 수정이 효과였을 가능성. 단 우회(PCM WAV 로 키우기)는 ffmpeg 이 있어야 하고 **이 서버엔 ffmpeg 이 없다** → 브리지 시작 로그에 ⚠ 경고 + DEPLOYMENT §STT (`017a41f`). WAF 가 다시 켜지면 답변 녹음(수백 KB)이 첫 희생자다 |
| 말소리 없는 녹음(톤만 든 WAV) | 벤더가 "인식 결과가 비어 있습니다" → 502 → 화면 「받아쓰기에 실패했어요: [skt-ax] 인식 결과가 비어 있습니다」. 장애처럼 읽힌다 → **422 no_speech** 「말소리를 못 알아들었어요. 마이크에 조금 더 가까이 다시 말하거나 타이핑으로 답해 주세요.」 |
| 마이크 안내 문구 | 「타이핑으로 답하셔도 됩니다」 3곳 — §3-1 금지 높임·합쇼체 → 「타이핑으로 답해도 돼요」 (`c718b9b`) |
| 확장자 | 브라우저 mime → `.webm/.m4a/.mp3/.wav/.ogg` 매핑, 서버 화이트리스트 9종, 모르는 값은 `.webm` 폴백. 이상 없음 |
| 못 본 것 | **실제 브라우저 webm 짧은 녹음의 인식률** — 이 서버엔 짧은 말소리 샘플·브라우저가 없다. 부스 리허설 때 한 번 눌러 본다 (10초 답변 → 글자가 채워지는지) |

## 2026-09-13 (오전) — 부족분 메우기: P1 재시도 · P4 마무리 · P5 채점표 (Claude 세션)

사람·키·녹음이 필요한 P1 근본·P3·P8 은 못 하고, 코드로 되는 셋을 했다.

| 문제 | 한 것 | 실측 | 커밋 |
|---|---|---|---|
| P1 A.X 불안정 | LLM 클라이언트가 읽기 타임아웃·연결 끊김·429·5xx 를 2초 뒤 **한 번** 재시도. 400 은 안 함 | 테스트 4 (타임아웃→OK · 503→OK · 2회 실패 → 예외 · 400 은 1회) | `be41b86` |
| P4 지어낸 인용 | 발화와 겹침 0.15 미만인 인용은 빈 문자열 → 기존 "근거 없는 aligned 는 폴백" 규칙이 강등 | 저장 그래프 13항목 F-11 재실행: **인용∈발화 1.0**, aligned 9 전부 발화 원문, 근거 없는 4 는 missing. 지어낸 문장이 화면에 나가지 않는다 | `0fd6bb7` |
| P5 심사관 검증 | `examples/rubric_human_sheet.py sheet|agree` — 사람 채점 CSV(심사관 점수는 숨은 열) + 항목별 정확·±1 일치율·편차 | 오늘 기준선 6벌에서 채점표 18행 `exports/rubric_human/sheet_20260913.csv` 생성. 팀원이 채우면 `agree` 한 줄 | `594a0c1` |
| (부수) | f11 이 지어낸 인용을 버리자 mock 경로가 전부 강등 → mock 정합 인용을 프롬프트의 실제 발화에서 가져오게 | pytest 826 | `c99f47f` `090d88e` |

**다음:** 채점표 18행을 팀원 2명이 채우면 `agree` 로 P5 를 닫는다. `#/new` 상황 추정 미리 채우기(P6)는 사용자 결정 대기.

## 2026-09-12~13 총정리 — 그래프 · 프롬프트 · 하네싱이 **어떻게** 바뀌었나 (전·후 대비)

> 아래의 날짜별 절들은 바퀴마다 기록한 것이고, 이 절은 그 하루(커밋 31개)를 **바뀐 것 중심**으로 다시 묶은 것이다.
> 원칙 한 줄: **프롬프트로 부탁만 해서 안 지켜지는 것은 코드가 받는다.** 채택된 프롬프트 변경은 하나, 코드로 못 박은 것은 여섯이다.

### 0. 지금 남아 있는 문제 — 심각도순 (증거 · 제품 영향 · 원인 · 상태)

| # | 문제 | 증거 (실측) | 사용자·데모에 미치는 영향 | 원인 | 상태 | 막고 있는 것 |
|---|---|---|---|---|---|---|
| P1 | **추론 백엔드가 A.X 하나뿐이고, A.X 가 불안정하다** | 벤치 9회 중 4회 `{"questions":[]}` 빈 응답(1.9초) · 판정 JSON 실패로 하네스 사망 1회 · 180초 게이트웨이 타임아웃 1회 · 같은 코드로 그래프 노드 7~16 | 부스에서 질문 대신 템플릿("라벨 — 설명해 주세요")이 뜨거나 판정이 오류로 끝날 수 있다. 관람객이 첫 화면에서 본다 | 이 서버에 Solar 키가 없음. A.X 는 json_mode 에서 빈 목록·비JSON 을 낸다 | **완화** (F-08 재시도 · 판정 error 행 · F-11 백스톱 · **09-13 LLM 클라이언트가 타임아웃·5xx 를 한 번 재시도** `be41b86`). 근본 해결 아님 | Solar 키 (Upstage) · A.X 조건 문의 (SKT) |
| P2 | **그래프 품질을 판별할 수 없다** | 같은 코드 3회: 노드 7/9/11 · 30분 뒤 3회 평균 15.7 · 요약 근거 0.30~0.66 | 개념 그래프가 발표마다 다른 크기로 나와 정합·질문·리포트의 대상이 널뛴다. 노드 7개면 발표의 절반이 평가 밖 | P1 과 같음. 프롬프트 가설 4개 전부 잡음 폭 안 | **미해결 · 보류** | P1 해결 또는 표본 5~10건 |
| P3 | **평가 세트가 발표 1건(집중력 교양 발표)뿐이다** | 질문 3개 기준 rubric 잡음 ±0.7 · 가설 8개 채택 0 · 페르소나 실험은 자료와 상황이 안 맞아 기각 | 무엇을 고쳐도 "좋아졌다"고 말할 수 없다. 결선 그래프(Baseline→…)의 원자료가 안 쌓인다 | 동의 세션 0건 · 팀 발표 녹음 없음 | **미해결** | 팀원 발표 녹음 5~10건 + 학습 동의 (사람) |
| P4 | **정합 인용이 발화가 아니었다** | A.X 인용 6개 중 5개가 개념 요약문 (인용∈발화 0.17) | 리포트가 발표자가 안 한 말을 「이렇게 말했어요」 로 보여 줬다 — 신뢰 문제 | 프롬프트 "그대로 인용" 을 A.X 가 안 지킴 | **해결** (`_verbatim_evidence` 0.17→0.77, **09-13 겹침 0.15 미만은 근거 없음으로 버리고 판정 강등** `0fd6bb7`) | 남은 것 없음 — 지어낸 인용은 화면에 나가지 않는다 |
| P5 | **rubric 심사관을 믿을 근거가 없다** | 같은 질문 3개에 4.69 vs 3.96 · id 누락 2/3 · 함정 질문 전 항목 1점 | 루프가 심사관 잡음으로 좋은 변경을 버리거나 나쁜 변경을 받을 수 있다 | 사람 채점과 일치율을 안 쟀다 | **완화** (3회 중앙값 · 순서 짝짓기 · 함정 제외). 검증 안 됨. **09-13 채점표·일치율 도구 준비** `examples/rubric_human_sheet.py` `594a0c1` | 사람 채점 20건 — `sheet` 로 뽑아 나눠 주면 된다 |
| P6 | **발표 상황이 파이프라인에 작용하지 않는다** | 상황 4종으로 재도 질문이 글자까지 같음 | "교수 대상" 이든 "상사 대상" 이든 같은 질문 — 회의가 약속한 맞춤이 없다 | 상황이 프롬프트 한 줄로만 실림. 페르소나 블록은 자료와 안 맞으면 개악 | **부분** (F-23 상황 추정 서버 완료, 화면 미연결) | `#/new` 미리 채우기 결정 · 상황 맞는 표본 |
| P7 | **질문·판정 프롬프트 본문이 하루 종일 한 글자도 안 바뀌었다** | 가설 8회 → 채택 0 | 개선 스토리(결선 §5)의 "Prompt 개선" 단계가 비어 있다 | P3·P5 의 결과 | **미해결** | P3 |
| P8 | 9/16 소개자료 담당이 비어 있다 | 회의록 할 일 #1, 3일 남음 | 제출 못 하면 부스 패널이 없다 | 사람 결정 | **초안만** (`FESTA_PANEL_DRAFT.md`) | 디자인 담당 확정 (사람) |

읽는 법: P1 이 P2·P4·P5 를 만들고, P3 이 P5·P7 을 만든다. **키 하나(P1)와 녹음 열 개(P3)가 오면 절반이 풀린다.** 나머지는 코드로 완화해 둔 상태다.

### A. 그래프·정합 (F-06 → F-07 → F-11)

| | 전 (09-12 아침) | 후 (09-13 새벽) | 근거 커밋 |
|---|---|---|---|
| 자 (측정) | 없음. "노드 20 vs 5" 같은 눈대중 (MODEL_BENCH_F07) | `examples/graph_eval.py` 지표 12개: 장 커버리지 · 근거 장 · 이름 근거 · 요약 근거 · 장당 노드 · 고립 · 끊긴 간선 · 중복 이름 · **인용∈발화** · 거짓 누락 · 거짓 정합 · 커버리지 | `81a5955` |
| 재는 방법 | — | `scripts/graph_bench.sh --stage all\|graph\|align --runs 3` (기본 3회 평균). F-11 가설은 그래프 고정 | `b214c1c` `7b35efe` |
| F-07 요약 프롬프트 (규칙 7) | `label 은 개념 이름만 짧게. summary 에 한 줄 설명을 넣는다.` | `summary 는 새 문장을 짓지 않는다 — 개념 목록 항목의 '개념명: 설명' 에서 설명 부분을 그대로 옮겨 적는다 … 목록에 없는 일반어(정신적·구조적·체계·에너지·요소·신호)로 풀어 쓰지 마라. 자료의 낱말만 쓴다.` | **`4e48ece`** (루프가 채택: 이름 근거 0.86→1.0 · 요약 근거 0.34→0.39 · 거짓 정합 2→0) |
| F-11 인용 (evidence) | 프롬프트 규칙 2 "발화에서 그대로 인용한다" 만 있고 검사 없음. A.X 는 **개념 요약문**을 인용 자리에 옮겼다 (6개 중 5개, 인용∈발화 0.17) | 코드 백스톱 `_verbatim_evidence`: 인용이 발화 원문이 아니면 그 개념 근거 장 발화에서 가장 겹치는 24낱말 창으로 바꾼다. 겹침 없으면 그대로(지표가 잡는다). 실측 인용∈발화 **0.17 → 0.77** | `b214c1c` |
| 노드 수 (A.X 7~8 / 12장) | 프롬프트 가설 4개(G3·G5·G6·G7)로 시도 | 전부 REGRESSED — 그러나 같은 코드 3회가 노드 7/9/11, 30분 뒤 3회 평균 15.7. **판별 불가 → 그래프 프롬프트 루프 보류.** 결정론 후보 G4(F-06 입력에서 캡션 제거 뒤 클립)는 표본이 생기면 첫 순서 | `31692b8` |

### B. 질문·판정 프롬프트 (F-08 · F-09) — 프롬프트는 안 바뀌고 **그 주변 코드**가 바뀌었다

| | 전 | 후 | 근거 커밋 |
|---|---|---|---|
| A.X 가 `{"questions":[]}` 를 돌려줄 때 | 파싱은 되니 정상으로 보고 전부 템플릿("라벨 — 설명해 주세요")으로 메움. 사용자는 그것만 받았다 (벤치 9회 중 4회) | `_questions_with_retry`: 대상 node_id 가 하나도 없으면 파싱 실패와 같이 보고 JSON_RETRY_NUDGE 를 붙여 한 번 더. 재시도 실측 12초에 정상 5개 | `5b2be5c` |
| 말투 | 프롬프트 규칙 "해요체" 만. A.X 는 "~되는가?" · "~무엇인가?" 로 냈다 (두 변형 3/3) | `_polite_question`: 어미만 해요체로 (는가→나요 · ㄴ받침+가→가요 · 까→까요 · 느냐→나요 · 하라→해 주세요). 본문·문장부호는 안 건드린다 | `7d62f43` `8d9fce4` |
| 상황(situation) | "발표 상황: work_report" 한 줄 — 4상황으로 재도 질문이 글자까지 같았다 | 페르소나 블록을 넣어 봤으나 4상황 중 3개 REGRESSED → **기각**. 대신 상황을 **자료에서 추정**하는 F-23(`suggest_context`, `/api/v1/suggest-context`) 을 서버에 넣었다 | `8e4e908` `caa9138` |
| 프롬프트 가설 H1~H4 (8회 시도) | — | 전부 REGRESSED 또는 A.X 이상. 유효한 기준선에서 잰 것: H2b(coverage ↓) · H3b(인용률 ↓) · H4(판정 JSON 실패가 지배). 채택 0 | 장부 `docs/QA_BENCH_LEDGER.md` |

### C. 하네싱 (측정·루프 인프라) — 하루 동안의 변천

| 순서 | 무엇이 생겼나 | 왜 (그 직전에 겪은 것) |
|---|---|---|
| 1 | `qa_eval_compare.py`: 결과 둘 → IMPROVED/REGRESSED/NOISE, 장부 | 전후를 눈으로 비교해 9/10 코칭 개선이 다른 지표를 깎은 걸 놓쳤다 |
| 2 | `/improve-qa` 워크플로 + 에이전트 4종 (재기·고치기·문지기·채점) | 사람이 매번 프롬프트를 고치고 읽는 건 안 된다 (회의 §4) |
| 3 | rubric 7항목 LLM 심사 · 번들 N개 평균(`*.corpus.json`) | 특이도·인용률은 "자료를 보는가"만 재고 "받을 법한 질문인가"는 못 쟀다 |
| 4 | `VERDICT: INVALID` (전부 폴백은 비교 안 함, 기준선은 재측정) | 루프 1 의 세 판정이 무효 기준선과 비교됐다 |
| 5 | 판정 실패를 error 행으로, 이상 시 `*.raw.json` 원문 덤프 | 루프 2 에서 하네스가 죽었고 원인을 볼 원문이 없었다 |
| 6 | `graph_eval.py` + `graph_bench.sh` + `/improve-graph` | 질문의 앞단에 자가 없었다 |
| 7 | `--stage align\|graph` 단계 고정 · `--stage graph` 면 옛 정합 폐기 | G1 이 그래프까지 다시 만들어 정합 지표가 잡음을 탔다 · G5 에서 7노드에 정합 13항목 |
| 8 | rubric 3회 중앙값 · `--situation` · `persona_bench.sh` | 같은 질문 3개에 종합 4.69 vs 3.96 · 픽스처에 상황이 없어 벤치가 전부 범용 |
| 9 | 그래프 `--runs 3` 평균 + 실측 잡음 폭 | 같은 코드 3회 노드 7/9/11 |
| 10 | 문지기 범위를 코드 폴더로 한정 | 루프 중 사람이 문서·도구를 못 고쳤다 |
| 11 | `docs/FAILURE_QUESTIONS.md` 진단 질문표 | 위 1~10 이 전부 "왜 안 되지" 에서 나왔다 |

### D. 숫자로 본 전·후 (같은 픽스처, A.X)

| 지표 | 전 | 후 | 무엇이 바꿨나 |
|---|---|---|---|
| 질문 폴백 (A.X 빈 응답 시) | 3/3 | 0/3 | F-08 재시도 |
| 반말 질문 | 3/3 | 0/3 | F-08 어미 후처리 |
| 정합 인용∈발화 | 0.17 | 0.77 | F-11 백스톱 |
| 하네스 생존 (판정 JSON 실패 시) | 죽음 | error 행 기록 후 계속 | qa_eval |
| 그래프 노드 (A.X, 같은 코드) | 7~8 로 알았음 | 7~16 으로 흔들림을 **알게 됨** | runs 3 실측 |

### E. 아직 안 바뀐 것 (정직하게)

- 질문·판정 프롬프트 본문은 09-12 아침과 **같다.** 8개 가설 중 채택 0 — 발표 1건·질문 3개로는 잡음 폭 밖으로 나오기 어렵다.
- 그래프는 A.X 로 프롬프트 효과를 판별할 수 없다. Solar 키 또는 표본 5~10건이 오기 전까지 그래프 루프는 돌리지 않는다.
- rubric 심사관이 사람 채점과 얼마나 맞는지는 아직 안 쟀다. 그 전까지 rubric 은 참고 지표다.

## 2026-09-12 (심야) — 소개자료 초안 · H4 재측정 · 그래프 루프 2차 · A.X 그래프 잡음 실측 (Claude 세션)

### 바뀐 것

- **부스 소개자료 내용·레이아웃 초안** `docs/FESTA_PANEL_DRAFT.md` (`86d74de`): 구역별 문안(해요체)·mm 수치·디자인 값(app.css 원본)·쓰지 않는 말·제출 체크리스트. 회의록 할 일 #1 → 🟡 (남은 것: 디자인 담당 → `.ai`).
- `graph_eval --runs 3` 기본 + 잡음 폭 실측값 (`7b35efe`) · `--stage graph` 면 저장 정합을 버림 (`fbc1fef`).
- 채택된 프롬프트 변경: **없음.**

### 잰 것과 결론

| 무엇 | 실측 | 결론 |
|---|---|---|
| H4(판정 골자 하한) 단독 루프 (2257) | 기준선 골자→통과 3/3 → 변형 2/3, 판정 JSON 실패 0→2 | REGRESSED. 이 표본에선 이길 여지가 없었고 A.X JSON 실패가 지배 |
| 그래프 루프 2차 (2304, stage=graph) G5·G4·G6 | 셋 다 요약 근거가 기준선 0.66 보다 낮아 REGRESSED (노드는 7→7/9/11) | 기준선 0.66 은 이상치였다 (아래) |
| **같은 코드 3회 재생성** (gnoise-a/b/c) | 노드 7/9/11 · 요약 근거 0.30/0.43/0.55 · 고립 0/0/0.18 | 한 번 재서 비교한 오늘 그래프 판정은 전부 폭 안. `--runs 3` 도입 |
| 3회 평균 기준선 vs G4(F-06 입력 정제) 3회 평균 | 기준선 노드 15.7·중복 2.67 → G4 노드 10.3·중복 0.67 | 규칙상 REGRESSED(장당 노드 ↓). 그러나 **30분 전 기준선 평균 9 → 지금 15.7** — A.X 그래프 출력은 3회 평균으로도 회차 간 노드 6개 차이 |

**결론:** A.X 로는 그래프(F-06/07) 프롬프트 변경의 효과를 지금 판별할 수 없다. 하네스는 그대로 두고, 그래프 프롬프트 루프는
**Solar 키가 오거나 A.X 출력이 안정될 때까지 보류**한다. 그 전까지 그래프 쪽 개선은 결정론 코드(F-11 인용 백스톱 같은 것)로만.
Q&A 쪽은 재시도·해요체 후처리·3회 rubric 으로 측정이 유효해졌지만, 오늘 8개 가설 중 채택 0 — 표본이 발표 1건이라 잡음 폭 밖으로
나오기 어렵다. **다음 개선의 병목은 프롬프트가 아니라 평가 세트(발표 5~10건)다.**

### 다음 세션

1. 평가 세트가 오면: `qa_bench.sh --bundle-dir` · `graph_bench.sh --bundle-dir` 로 N건 평균 비교 (코드는 준비돼 있다).
2. `#/new` 폼에 F-23 상황 추정 미리 채우기 — 사용자 확인 뒤.
3. F-23 내용 제안(B6) 서버 쪽.
4. G4(F-06 입력 정제)는 결정론 개선이라 표본이 생기면 첫 후보로 다시 잰다 — 중복 이름 쌍 2.67→0.67 은 신호였을 수 있다.

## 2026-09-12 — 개념 그래프·정합 프롬프트 자율 루프 (improve-graph, 20260912-2304)

`improve-graph` 루프를 돌렸다. F-07 합병 기준(G5)·F-06 캡션 정제(G4)·F-06 장당 개념 수(G6) 세 가설을 잰 결과 셋 다 REGRESSED 라 채택은 없다.
장부: [GRAPH_BENCH_LEDGER.md](GRAPH_BENCH_LEDGER.md) · 리포트: [`exports/graph_eval/reports/`](../exports/graph_eval/reports/) (`…gbase-20260912-2304__vs__…g1~g3-20260912-2304.md`).

**기준선 `gbase-20260912-2304`:** 노드 7 · 간선 7 · 장 커버리지 1.0 · 이름 근거 1.0 · 요약 근거 0.66 · 고립 0.14 · 끊긴 간선 0 · 중복 쌍 0 · (정합 7항목 aligned 7) 인용∈발화 0.86 · 거짓 누락 0 · 거짓 정합 0 · 커버리지 1.0.

| 가설 | 제목 | 판정 | 핵심 델타 |
|---|---|---|---|
| G5 (`g1-20260912-2304`) | F-07: 노드 수 기준을 '피할 숫자'에서 '후보 대비 근거 있는 목표'로 바꾸고, 따로 둔 노드마다 parent 간선을 묶는다 (`f07_graph.py` SYSTEM_PROMPT 세 번째 불릿 교체) | REGRESSED | 노드 7 그대로 · 간선 7→6 ▼ · 장 커버리지 1.0→0.83 ▼ · 요약 근거 0.66→0.35 ▼ · 고립 0.14→0.0 ▲ · 정합 13항목 aligned 13 · 인용∈발화 0.86→1.0 ▲ · 거짓 누락 0 · 거짓 정합 0 |
| G4 (`g2-20260912-2304`) | F-06: 슬라이드 본문에서 이미지 캡션을 걷어낸 뒤 클리핑한다 (`f06_concepts.py` `_build_user_prompt` 에 `clean_slide_text` 한 줄) | REGRESSED | 노드 7→9 ▲ · 간선 7→13 ▲ · 섹션 1→5 · 요약 근거 0.66→0.45 ▼ · 고립 0.14→0.0 ▲ |
| G6 (`g3-20260912-2304`) | F-06: 장당 개념 2~5개, 번호·단계·표 행·항목 목록은 항목 하나가 개념 하나 — 개념명은 자료 낱말 그대로 (`f06_concepts.py` SYSTEM_PROMPT 규칙 2 확장) | REGRESSED | 노드 7→11 ▲ · 간선 7→28 ▲ · 장당 노드 0.58→0.92 ▲ · 요약 근거 0.66→0.37 ▼ · 고립 0.14→0.0 ▲ · 중복 이름 쌍 0→1 ▼ · 정합 미측정(`--stage graph`) |

채택 없음 — 다음에 볼 가설: 세 가설 모두 노드·고립은 나아졌는데 요약 근거(summary_grounding)가 0.66→0.35~0.45 로 같이 무너졌다. 노드를 늘리면 F-07 이 설명을 자료 문장에서 옮기지 않고 지어내는 쪽으로 기우는 것으로 보이니, 개수 지시보다 먼저 규칙 7(설명은 자료 문장 그대로)을 강제하는 쪽 — 요약을 후보 설명에서 복사하도록 못 박거나 후보 설명 자체를 자료 낱말로 짧게 만드는 가설 — 을 단독으로 잰 뒤에 G4·G6 을 다시 얹는다. G5 는 노드 수가 7 에서 안 움직였으니 A.X 가 개수 지시에 둔감하다는 G3 때 관찰이 다시 확인된 셈이라 같은 방향으로는 더 안 간다.

---

## 2026-09-12 — Q&A 프롬프트 자율 루프 (improve-qa, 20260912-2257)

`improve-qa` 루프를 네 번째로 돌렸다. 2208 루프에서 게이트웨이 타임아웃으로 못 잰 H4(판정 골자 하한)를 판정 켠 채로 다시 쟀고, 채택은 없다.
장부: [QA_BENCH_LEDGER.md](QA_BENCH_LEDGER.md) · 리포트: [`exports/qa_eval/reports/`](../exports/qa_eval/reports/) (`…base-20260912-2257__vs__…v1-20260912-2257.md`).

**기준선 `base-20260912-2257`:** 질문 3개 · 폴백 0 · 함정 1 · 특이도 평균 4.0 · 인용률 평균 0.64 · 말투 위반 0(높임 질문 0·판정 0·반말 0/3) · rubric 종합 4.09 · 환각 0 (groun 4.5·relev 5.0·cover 3.0·depth 3.0·answe 4.67·non_d 4.67) · 판정 4벌: 골자→통과 3/3 · 엉뚱→wrong 3/3 · 함정동의→wrong 1/1 · 함정정정→통과 1/1.

| 가설 | 제목 | 판정 | 핵심 델타 |
|---|---|---|---|
| H4-gist-floor-70 | 골자의 뜻을 다 담은 답은 근거가 얕아도 70 아래로 내리지 않는다 — missing_points 는 골자 안에서만 (`f09_judge.py` SYSTEM_PROMPT 규칙 8 에 네 줄 추가, 코드 가드·상수는 그대로) | REGRESSED | 판정 4벌 골자→통과 3/3→2/3 ▼ · 엉뚱→wrong 3/3→2/3 ▼ · 함정동의 1/1 · 함정정정 1/1 · judge_errors 0→2 ▼ · 판정 높임 0→1 ▼ · 특이도 4.0→4.67 ▲ · 인용률 0.64→0.55 ▼ · rubric 종합 4.09→4.36 ▲ · 폴백 0 · 환각 0 |

채택 없음 — 다음에 볼 가설: H4 는 올리려던 골자 통과가 오히려 떨어졌고(3/3→2/3) 지키려던 엉뚱→wrong 도 떨어졌다(3/3→2/3). 다만 judge_errors 가 0→2 로 같이 늘었으니 프롬프트가 나빠진 것인지 판정 JSON 파싱 실패가 표본을 깎은 것인지 아직 가를 수 없다 — 2208 루프의 결론과 같이 판정 파서(JSON 실패 시 재시도)를 먼저 손본 뒤 같은 문장으로 한 번 더 잰다. 질문 생성(F-08)은 안 바꿨는데 특이도·인용률·rubric 이 움직인 것은 심사관 잡음 범위로 본다.

---

## 2026-09-12 (밤) — 페르소나 실험은 기각, 상황 추정(F-23)은 채택, 심사관 잡음은 중앙값으로 (Claude 세션)

사용자 질문 "상사 조건은 왜 점수가 뚜렷이 낮나" 에서 출발했다.

### 확인한 것

| 무엇 | 실측 | 결론 |
|---|---|---|
| 상황 4종으로 잰 기준선 질문 | 교수·상사 조건의 질문 3개가 **글자까지 같았다** | 상황 한 줄은 질문 생성에 작용하지 않는다 |
| 같은 질문의 rubric 점수 | 한 번 채점: 종합 4.69 vs 3.96 (coverage 3 vs 2 · depth 4 vs 2) | 심사관 한 번 채점은 ±0.7 잡음. **3회 중앙값**으로 바꿈 (`6d9918f`) |
| A.X 나쁜 첫 응답의 원문 (`*.raw.json`, 처음 남음) | `{"questions":[]}` 를 1.9초에. 재시도(nudge)는 12초에 정상 5개 | `_questions_with_retry`(`5b2be5c`) 가 맞는 처방이었다 |
| 페르소나 블록 (로드맵 B3) 4상황 비교, 각 rubric 3회 | 질문은 상황별로 달라졌으나 4상황 중 3개 REGRESSED (고객 조건 종합 4.16→3.22) | **기각.** 이 픽스처(집중력 교양 발표)에 고객·상사 페르소나는 안 맞는다 — 안 맞는 상황을 씌우면 질문이 나빠진다 |

### 바뀐 것

- `chuckchuck/f23_context.py` + `contracts.ContextSuggestion` + `POST /api/v1/suggest-context` (`8e4e908`): 자료의 장 제목·본문 신호로
  상황을 추정한다. 결정론, 호출 0. 신호가 약하면 미정 — 화면은 아무것도 안 채운다. 화면 미리 채우기(A6)는 사용자 확인 뒤.
- `examples/qa_eval.py --rubric-runs 3` 중앙값 · `--situation/--audience` 덮어쓰기 · `scripts/persona_bench.sh`(4상황 비교, 하나라도 REGRESSED 면 전체).
- `f08 _polite_question`: ㄴ받침+가 일반화 ("다른가"→"다른가요").
- 설계: `docs/plan/deck-tailored-suggestions.plan.md` (상황 추정 + 청중이 기대하는 내용의 빈칸 찾기 F-23/B6).
- 루프 3(20260912-2208, 판정 켬): H2b·H3b 유효 기준선에서 REGRESSED, H4 는 A.X 타임아웃. 채택 0.

### 남은 것

- 페르소나 블록은 **상황이 자료와 맞을 때** 다시 잰다 — 업무 보고 자료 번들이 필요하다 (회의 할 일 #2 와 같은 병목).
- rubric 심사관에게 상황을 알려 줄지: 지금은 자료 품질만 보므로 페르소나 질문이 불리할 수 있다. 사람 채점 20건과 대조한 뒤 결정.
- H4(판정 골자 하한)는 아직 한 번도 유효하게 재지 못했다.

## 2026-09-12 — Q&A 프롬프트 자율 루프 (improve-qa, 20260912-2208)

`improve-qa` 루프를 세 번째로 돌렸다. 질문 재시도(`_questions_with_retry`)가 들어간 뒤 처음으로 폴백 0 인 유효 기준선 위에서 가설 3개를 하나씩 쟀고, 채택은 없다.
장부: [QA_BENCH_LEDGER.md](QA_BENCH_LEDGER.md) · 리포트: [`exports/qa_eval/reports/`](../exports/qa_eval/reports/) (`…base-20260912-2208__vs__v1…`, `…__vs__v2…`).

**기준선 `base-20260912-2208`:** 질문 3개 · 폴백 0 · 함정 1 · 특이도 4.33 · 인용률 0.70 · 말투 위반 0/0/0 · rubric 종합 4.36 · 환각 0 · 판정 4벌: 골자→통과 2/3 · 엉뚱→wrong 2/3 · 함정동의→wrong 1/1 · 함정정정→통과 1/1 (판정 오류 1: q02 엉뚱한 답에서 JSON 파싱 실패).

| 가설 | 제목 | 판정 | 핵심 델타 |
|---|---|---|---|
| H2b-coverage-deck-not-said | aligned 개념의 심화 질문을 「자료 본문에는 있는데 발표에서 안 한 말」로 겨냥한다 (`f08_questions.py` 규칙 4, coverage 3.67 재측정) | REGRESSED | 특이도 4.33→3.67 ▼ · coverage 3.67→3.0 ▼ · depth 4→2.67 ▼ · rubric 종합 4.36→3.97 ▼ · 반말 질문 0→1/3 ▼ · 판정 높임 0→1 ▼ · 인용률 0.71 · 폴백 0 · 환각 0 · 판정 4벌 골자 3/3 · 엉뚱 3/3 · 함정동의 1/1 · 함정정정 1/1 |
| H3b-concrete-item-in-question | 질문 문장에 자료 본문의 구체 항목 하나를 그대로 넣고 그것의 이유·한계·비교를 묻게 한다 (question 필드 설명, 중복 금지 포함) | REGRESSED | 특이도 4.33→5.0 ▲ 이지만 인용률 0.70→0.42 ▼ · 폴백 0 · 말투 위반 0 · 판정 4벌 골자 3/3 · 엉뚱 3/3 · 함정동의 1/1 · 함정정정 1/1 (judge_errors 1→0) · rubric 은 심사 JSON 파싱 실패로 3개 전부 누락(종합 None) |
| H4-judge-gist-floor | 골자와 뜻이 같은 답은 세부가 빠졌어도 70 아래로 내리지 않는다 — 함정을 바로잡은 답도 같은 규칙 (`f09_judge.py` 규칙 8, gist_passed 2/3 재측정) | ERROR | `qa_bench.sh` 가 VERDICT 없이 종료 — F-08 질문 생성 중 A.X 게이트웨이(awf-gw.adot.ai) 읽기 타임아웃(180초). 결과 JSON·리포트 미생성. 과금 규칙대로 재시도하지 않음 |

채택 없음 — 다음에 볼 가설: H4 는 프롬프트 탓이 아니라 게이트웨이 타임아웃으로 못 잰 것이니 그대로 다시 잰다. H3b 는 특이도가 올랐지만 인용률이 크게 떨어졌으니 「구체 항목을 자료 본문 표현 그대로 넣는다」를 더 강하게 못 박은 변형으로, H2b 는 coverage·depth 가 함께 떨어졌으니 규칙 4 를 통째로 바꾸기보다 「발표에서 안 한 말」 선택 기준만 한 줄 남긴 축소판으로 본다. 기준선의 판정 오류 1(q02 엉뚱한 답 JSON 파싱 실패)과 H3b 의 rubric 전부 누락은 같은 심사 JSON 문제라 프롬프트보다 파서 쪽을 먼저 본다.

---

## 2026-09-12 — 자율 개발 환경 구축 + 첫 세 바퀴 (Claude 세션, 총괄 — 무엇이 바뀌었나)

사용자 지시: 회의록 문서화 → 멀티 에이전트 자율 개선 환경 → "혼자 계속 디벨롭" → "하네싱·그래프 엔지니어링으로
내부 AI 셋팅을 더 효과적으로". 아래 세 절(루프 기록)은 워크플로가 쓴 것이고, 이 절은 사람이 하루를 정리한 것이다.
커밋 15개 (`26ee93f` … `b214c1c`). pytest 756 → **808**.

### 1. 새로 생긴 것

| 무엇 | 어디 | 한 줄 |
|---|---|---|
| 회의록 폴더 | `docs/회의록/` | 회의 목록 · **살아 있는 할 일 9건** · 마일스톤(9/16 소개자료 · 10/6~8 Festa · 11/18 결선). 템플릿 포함 |
| 자율 개발 헌장 | `docs/ROADMAP_AUTONOMOUS.md` | 세 갈래(A 데이터 · B Q&A 하네스 · B' 그래프 · C 뉘앙스 UI) · 매 세션 절차 · 하지 않는 것. **Festa 전 프론트 새 화면 금지, 동의한 업로드만** |
| Q&A 벤치마크 판정 | `examples/qa_eval_compare.py` | 결과 둘 → IMPROVED/REGRESSED/NOISE. 1차 지표 24개 중 하나라도 잡음 밖으로 나빠지면 REGRESSED. 장부 `docs/QA_BENCH_LEDGER.md` |
| rubric 7항목 심사 | `examples/qa_eval.py --rubric` | 회의 §4 의 Groundedness…Hallucination 을 LLM 심사관 1회 호출로. 함정 질문은 groundedness 채점 제외 |
| 번들 N개 평균 | `qa_eval.py --bundle-dir` → `*.corpus.json` | 단일 요약과 같은 모양이라 비교 스크립트가 그대로 읽는다 |
| Q&A 자율 루프 | `/improve-qa` · `.claude/workflows/improve-qa.js` · 에이전트 4종 | 기준선 → 가설 → [고치기 → 문지기 → 벤치 → 채택/되돌림] → 작업일지. 변형 상한 5, push 기본 꺼짐 |
| 그래프·정합 하네스 | `examples/graph_eval.py` · `scripts/graph_bench.sh` | 결정론적 지표 12개(장 커버리지·이름/요약 근거·고립·끊긴 간선·중복·**인용∈발화**·거짓 누락/정합). `--stage all\|graph\|align` |
| 그래프 자율 루프 | `.claude/workflows/improve-graph.js` · 에이전트 2종 | improve-qa 와 같은 구조, 대상 f06/f07/f11. 장부 `docs/GRAPH_BENCH_LEDGER.md` |
| 학습 자산 자동 갱신 | `demo/learning_jobs.py` + 브리지 하루 1회 스레드 | 동의 세션 → `var/data/derived/` 평가 묶음·또래 표. **0건이면 안 쓴다** (손으로 만든 exports 를 안 덮는다) |
| Holdout 자리 | `fixtures/holdout/README.md` | 튜너·가설 에이전트가 읽지 않는 최종 검증 세트. 아직 비어 있음 |
| 페르소나 설계 노트 | `docs/plan/qa-persona-questions.plan.md` | 상황별 「묻는 사람」 블록 (로드맵 B3). 픽스처에 상황이 없어 측정 도구부터 |

### 2. 파이프라인 코드가 바뀐 것 (사용자 화면에 닿는 변경)

| 파일 | 바뀐 것 | 왜 (실측) |
|---|---|---|
| `chuckchuck/f08_questions.py` | `_polite_question`: LLM 이 반말 어미로 낸 질문을 해요체로 (되는가→되나요 · 인가→인가요 · 까→까요 · 하라→해 주세요). 어미만 바꾼다 | 루프 1 두 변형 모두 질문 3/3 반말. 프롬프트 규칙 5 는 안 지켜졌다 |
| `chuckchuck/f08_questions.py` | `_questions_with_retry`: 대상 node_id 가 하나도 없는 JSON 은 파싱 실패와 같이 보고 한 번 더 묻는다 | A.X 가 0.9~18초에 대상 id 없는 JSON 을 돌려준 일이 벤치 9회 중 4회. 그동안 사용자는 "라벨 — 설명해 주세요." 템플릿만 받았다. 재시도 뒤 실측 폴백 0 |
| `chuckchuck/f07_graph.py` (`4e48ece`, 루프가 채택) | summary 를 새로 짓지 않고 개념 목록의 설명을 그대로 옮긴다 | 이름 근거 0.86→1.0 · 요약 근거 0.34→0.39 · 거짓 정합 2→0 (IMPROVED) |
| `chuckchuck/f11_align.py` | `_verbatim_evidence`: 정합 인용이 발화 원문이 아니면 근거 장 발화의 가장 겹치는 24낱말 창으로 바꾼다. 겹침 없으면 그대로(지표가 잡는다) | A.X 인용 6개 중 5개가 개념 요약문이었다(인용∈발화 0.17). 백스톱 뒤 저장 그래프 위 실측 **0.77** |
| `demo/bridge.py` | 만료 정리 스레드가 학습 자산도 갱신 | 로드맵 A2 |
| `examples/qa_eval.py` | 판정·코칭 실패를 error 행으로 기록하고 계속 간다 (`judge_errors`). 전부 폴백·판정 오류·rubric 누락·**질문 재시도**가 있으면 원문을 `*.raw.json` 으로 남긴다 | 루프 2 에서 판정 JSON 실패로 하네스가 통째로 죽었고, 원인을 볼 원문이 없었다 |

### 3. 하네스 자체가 배운 것 (판정 규칙 변경)

- **전부 폴백인 측정은 INVALID** — 기준선은 한 번 재측정, 변형은 판정 없이 되돌림 (`qa_bench.sh`). 루프 1 의 세 판정이 무효 기준선과 비교됐던 사고에서.
- rubric 잡음 폭 0.3 → **0.5** (질문 3개면 1점 차 = 0.33 은 잡음). 심사관이 id 를 바꿔 쓰면 순서로 짝짓기.
- F-11 가설은 **그래프 고정**(`--stage align`)으로 잰다 — G1 이 F-06/07 까지 다시 돌아 노드 7→10 이 되면서 정합 지표가 잡음을 탔다.
- 문지기 범위 검사는 코드 폴더(chuckchuck·tests·fixtures·demo)만 — 루프가 도는 동안 문서·도구 작업이 막혔다.

### 4. 오늘의 숫자 (장부에서)

| 루프 | 기준선 | 변형 | 채택 |
|---|---|---|---|
| improve-qa 1643 | **무효**(전부 폴백) | H1·H2·H3 전부 REGRESSED (반말 3/3 이 원인) | 0 |
| improve-qa 1730 | 유효 (특이도 3.67 · 인용률 0.70 · rubric 4.47) | H2b INVALID · H3b 판정 JSON 실패 · H4 INVALID | 0 |
| improve-graph 1801 | 노드 7 · 요약 근거 0.34 · 인용∈발화 0.17 | G1 REGRESSED · **G2 IMPROVED** · G3 REGRESSED | 1 (`4e48ece`) |
| 재시도 뒤 Q&A 기준선 1800 | 폴백 0 · 특이도 4.33 · 판정 골자→통과 3/3 | — | — |

세 바퀴가 채택한 프롬프트 변경은 하나지만, 루프가 **드러낸 결함**을 코드로 고친 것이 여섯이다. 프롬프트로 부탁만 해서 안 지켜지는
것은 코드가 받는다 — 오늘의 결론.

### 5. 알게 된 제약

- 이 서버의 추론 백엔드는 **A.X 뿐**이다 (Solar 키 없음, EXAONE 은 사고 과정을 뱉음). 저장 픽스처 그래프는 Solar 로 만든 것(노드 13)이라
  A.X 재생성(노드 7~8)과 직접 비교하면 안 된다. Solar 키가 오면 `graph_eval --rebuild --llm solar` 로 같은 자로 잰다.
- 픽스처에 발표 상황(context)이 없어 지금까지의 벤치는 전부 "범용 발표" 조건이다.

### 6. 다음 세션이 이어받을 것

1. `/improve-qa` 를 재시도 코드 위에서 다시 (judge 켬). 가설 H2b/H3b/H4 는 아직 유효한 기준선으로 재지 못했다.
2. `qa_eval.py --situation` 덮어쓰기 → 페르소나 블록(B3) 가설 1개.
3. A.X 의 노드 7~8/12장 — F-06 배치 크기·F-07 합병 규칙 가설 (`--stage graph`).
4. Eval Set 5~10건(회의 할 일 #2) — 팀 발표 녹음이 오면 `exports/eval_bundles/` 에.

## 2026-09-12 — 개념 그래프·정합 프롬프트 자율 루프 (improve-graph, 20260912-1801)

`improve-graph` 루프를 한 번 돌렸다. F-07(개념 그래프)·F-11(정합) 프롬프트 가설 3개를 기준선과 하나씩 비교했고, G2 하나를 채택했다.
장부: [GRAPH_BENCH_LEDGER.md](GRAPH_BENCH_LEDGER.md) · 리포트: [`exports/graph_eval/reports/`](../exports/graph_eval/reports/)
(`20260912-174825_gbase-20260912-1801__vs__*` 2벌 · `20260912-180026_g2-20260912-1801__vs__*g3*` 1벌 — G3 는 채택된 G2 위에서 쟀다).

**기준선 `gbase-20260912-1801`:** 노드 7 · 간선 7 · 장 커버리지 1.0 · 이름 근거 0.86 · 요약 근거 0.34 · 고립 0.0 · 끊긴 간선 0 · 중복 쌍 0 ·
(정합 7항목: aligned 6 / missing 1) 인용∈발화 0.17 · 거짓 누락 0 · 거짓 정합 2 · 커버리지 0.88.

| 가설 id | 제목 | 판정 | 핵심 델타 |
|---|---|---|---|
| G1-evidence-from-speech-only | F-11: evidence 는 「슬라이드별 발화」 절에서만 복사 — 개념 목록의 한 줄 설명을 옮기면 버려진다 | REGRESSED | 인용∈발화 0.17→0.0 ▼ · 거짓 정합 2→5 ▼ · 노드 7→10 · 간선 7→33 · 이름 근거 0.86→1.0 · 정합 커버리지 0.88→1.0. 발화 인용을 강제했더니 evidence 가 발화에 더 안 맞는 쪽으로 갔다 |
| G2-summary-copy-concept-line | F-07: summary 는 새로 짓지 말고 개념 목록 항목의 설명 문장을 그대로 옮긴다 | **IMPROVED (채택)** | 요약 근거 0.34→0.39 ▲ · 이름 근거 0.86→1.0 ▲ · 인용∈발화 0.17→1.0 ▲ · 거짓 정합 2→0 ▲ · 정합 커버리지 0.88→1.0 · 노드 8 · 간선 13 · 고립 0 · 중복 쌍 0 유지 |
| G3-merge-only-same-name | F-07: 합치는 것은 이름이 같은 항목뿐 — 고유 이름(영문 용어·연구자·N단계)은 각자 노드, label 은 목록 이름 그대로 | REGRESSED | 고립 0→0.12 ▼ · 간선 13→8 · 섹션 3→5 · 노드 8 그대로(기대한 11~15 에 못 미침) · 나머지 지표 동일 |

**채택 커밋:** `4e48ece` feat(graph-prompt): F-07: summary 는 새로 짓지 말고 개념 목록 항목의 설명 문장을 그대로 옮긴다 — `chuckchuck/f07_graph.py` 만 변경(+3/-1), 푸시하지 않았다.
G2 는 요약 근거 자체보다 정합(F-11) 지표가 크게 움직였다 — F-11 이 summary 를 인용해 쓰고 있어서, summary 가 자료 낱말로 바뀌자 evidence 가 발화와 맞기 시작한 것으로 본다.
다음에 볼 것: G3 은 노드 분리가 아니라 간선만 줄었으니(고립 발생) 간선 규칙과 함께 다시 쓴다. G1 은 F-11 이 summary 를 인용하는 경로를 먼저 끊은 뒤에 재측정한다.

## 2026-09-12 — Q&A 프롬프트 자율 루프 (improve-qa, 20260912-1730)

`improve-qa` 루프를 다시 돌렸다. 앞 루프(1643)에서 고친 폴백 INVALID 판정·해요체 후처리·rubric 순서 짝짓기가 적용된 첫 루프다.
기준선은 유효(폴백 0)했지만, 변형 3개가 전부 실행 단계에서 ERROR 로 끝나 비교 자체가 성립하지 않았다.
장부: [QA_BENCH_LEDGER.md](QA_BENCH_LEDGER.md) · 리포트: [`exports/qa_eval/reports/`](../exports/qa_eval/reports/) (이번 루프는 비교 리포트가 생성되지 않았다).

**기준선 `base-20260912-1730`:** 질문 3개 · 폴백 0 · 함정 1 · 특이도 3.67 · 인용률 0.70 · 말투 위반: 높임 질문 0 / 판정 1 / 반말 0/3 ·
rubric 종합 4.47 · 환각 0 (groun 4.0 · relev 5.0 · cover 3.67 · depth 4.0 · answe 5.0 · non_d 5.0) ·
판정: 골자→통과 2/3 · 엉뚱→wrong 3/3 · 함정동의→wrong 1/1 · 함정정정→통과 1/1.

| 가설 id | 제목 | 판정 | 핵심 델타 |
|---|---|---|---|
| H2b-coverage-deck-not-said | aligned 개념의 심화 질문을 「자료 본문에는 있는데 발표에서 안 한 말」로 겨냥한다 (유효 기준선으로 H2 재측정) | ERROR (INVALID) | 질문 3개 전부 폴백 — F-08 이 0.9초에 응답(LLM 응답 이상). 특이도 3.67→3.33 · 인용률 0.70→0.5 · rubric 파싱 실패(누락 3) · 함정정정→통과 1/1→0/1. 비교에 쓰지 않음, 리포트 미생성 |
| H3b-concrete-item-in-question | 질문 문장에 자료 본문의 구체 항목 하나를 그대로 넣고 그것의 이유·한계·비교를 묻게 한다 (H3 재측정, 중복 금지 한 구절 포함) | ERROR | `qa_bench.sh` 가 F-09 판정 단계에서 종료(`JudgeError: LLM 응답에서 판정 JSON 을 찾지 못했습니다`). VERDICT·결과 JSON·리포트 없음. 과금 규칙에 따라 재시도하지 않음 |
| H4-judge-gist-floor | 골자와 뜻이 같은 답은 세부가 빠졌어도 70 아래로 내리지 않는다 — 함정을 바로잡은 답도 같은 규칙 (판정 일관성) | ERROR (INVALID) | 질문 3개 전부 폴백이라 비교 불가. 판정만 보면 골자→통과 2/3→3/3 (목표 방향) · 엉뚱→wrong 3/3 · 함정동의→wrong 1/1 · 함정정정→통과 1/1→0/1 ▼ |

채택 없음 — 다음에 볼 가설: 세 변형이 모두 프롬프트 내용이 아니라 **LLM 호출 실패**(F-08 즉시 폴백 2회, F-09 판정 JSON 없음 1회)로 죽었다.
같은 시간대에 기준선만 정상이었으니 프롬프트 변경보다 제공자 쪽 불안정을 먼저 의심한다 — 루프 전에 F-08/F-09 를 한 번씩 예열 호출해 응답이 정상인지 확인하는 단계를 `qa_bench.sh` 에 넣고,
INVALID 가 아닌 ERROR(판정 JSON 실패)도 결과 JSON 을 남기게 한 뒤 H2b·H3b·H4 를 같은 순서로 다시 잰다. H4 는 폴백 질문에서도 골자→통과 3/3 이 나왔으므로 재측정 1순위다.

## 2026-09-12 — Q&A 프롬프트 자율 루프 (improve-qa, 20260912-1643)

`improve-qa` 루프를 한 번 돌렸다. 가설 3개를 기준선과 하나씩 비교했고, 셋 다 REGRESSED 라 채택하지 않았다.
장부: [QA_BENCH_LEDGER.md](QA_BENCH_LEDGER.md) · 리포트: [`exports/qa_eval/reports/`](../exports/qa_eval/reports/)
(`20260912-171643_base-20260912-1643__vs__*` 3벌).

**기준선 `base-20260912-1643`:** 질문 3개 · 폴백 3 · 특이도 평균 3.33 · 인용률 평균 0.5 · 말투 위반 0(높임 0·판정 0·반말 0/3) ·
rubric 종합 4.14 · 환각 0 (groun 4.0 · relev 4.67 · cover 3.0 · depth 3.67 · answe 4.67 · non_d 5.0) · 판정 미실행.
기준선 질문 3개가 전부 폴백 템플릿이라, 기준선 점수 자체가 템플릿 문장을 채점한 값이다.

| 가설 id | 제목 | 판정 | 핵심 델타 |
|---|---|---|---|
| H1-node-id-list | 질문 대상 node_id 목록을 프롬프트에 한 줄로 명시해 폴백(3/3)을 없앤다 | REGRESSED | 폴백 3→0 · 특이도 3.33→4 · 인용률 0.50→0.59 로 좋아졌지만 반말 질문 0→3/3, rubric 종합 4.14→3.66 (relev 4.67→3.67 · depth 3.67→2.67 · answe 4.67→3.67). 환각 0 |
| H2-coverage-gap | aligned 개념의 심화 질문을 「자료 본문에는 있는데 발표에서 안 한 말」에 겨냥한다 | REGRESSED | 폴백 3→0 · 특이도 3.33→4.67 · 인용률 0.50→0.52 · rubric 종합 4.14→4.40(＝) · coverage 3.0→3.0(＝, 목표 미달). 반말 질문 0→3/3 이 판정 원인. rubric 누락 2/3 · 환각 0 |
| H3-concrete-item-depth | 질문 문장에 자료 본문의 구체 항목 하나를 넣고 그것의 이유·한계·비교를 묻게 한다 | REGRESSED | 폴백 3→0 · 특이도 3.33→5.0 · depth 3.67→4.0 · rubric 종합 4.14→4.19(＝) · 인용률 0.50→0.46(＝) · 말투 위반 0. non_duplication 5→4.67 ▼ 이 판정 원인. 환각 0 |

채택 없음 — 다음에 볼 가설: 폴백이 걷히자 반말 질문(3/3)이 드러났으니, H1 의 node_id 명시에 **해요체 강제** 규칙을 함께 붙여
말투 위반을 0 으로 만든 뒤 H2/H3 를 다시 잰다. 그 전까지 기준선의 rubric 값은 템플릿 문장 점수라 비교 기준으로 믿기 어렵다.

### 루프가 끝난 뒤 사람이 고친 것 (같은 날, Claude 세션)

세 판정은 전부 **무효 기준선**과 비교한 것이다. 루프가 드러낸 결함 셋을 코드로 못 박았다.

| 드러난 것 | 고친 것 | 어디 |
|---|---|---|
| 기준선 질문 3개가 전부 폴백 — 질문 생성 호출이 1.1초(정상 20초대)에 끝났다. LLM 응답 이상을 프롬프트 점수로 읽었다 | 전부 폴백이면 `VERDICT: INVALID`. 기준선이면 한 번 재측정, 변형이면 판정 없이 되돌림 | `scripts/qa_bench.sh` · `.claude/workflows/improve-qa.js` |
| 폴백이 걷히자 LLM 질문이 반말("~되는가." · "~무엇인가?") 3/3. 프롬프트 규칙 5(해요체)는 지켜지지 않는다 | `_polite_question` — 어미만 해요체로 바꾸는 결정론적 후처리 (되는가→되나요 · 인가→인가요 · 까→까요 · 하라→해 주세요). 문장부호·본문은 안 건드린다 | `chuckchuck/f08_questions.py` · `tests/test_f08_polite.py` |
| rubric 심사관이 질문 id 를 바꿔 써서 2/3 누락 (v2) | 개수가 같으면 순서로 짝짓는다. 다르면 여전히 missing | `examples/qa_eval.py parse_rubric` |
| 질문 3개에서 한 질문 1점 차(평균 0.33)가 rubric 잡음 폭 0.3 을 넘어 REGRESSED (v3 non_duplication 5→4.67) | rubric 잡음 폭 0.5 — 두 질문이 같은 방향이어야 신호 | `examples/qa_eval_compare.py` |

같은 세션에 로드맵 A2 도 넣었다: 브리지의 하루 한 번 스레드가 만료 정리 뒤 동의 세션으로 평가 묶음·또래 표를
`var/data/derived/` 에 다시 만든다 (`demo/learning_jobs.py`). 동의 세션 0건이면 아무것도 쓰지 않는다 —
손으로 만든 `exports/eval_bundles/` 를 빈 것으로 덮지 않기 위해서다. `examples/build_eval_bundle.py`·`build_peer_norms.py` 는 CLI 껍데기로.

검증: pytest 798 (+21). 다음: 고친 루프로 `judge=true` 한 바퀴 (사용자가 과금 무관하다고 했다).

## 2026-09-10 — 업로드를 학습 자산으로: 동의 기반 세션 보관소 + 개선 루프 (Claude 세션, 같은 날 3차)

사용자 요청: "사용자가 올린 PPT/PDF 를 모아 우리 시스템을 학습·개선하고 싶다. 업로드 저장
디렉터리를 만들자. 날짜순으로 정렬되게." 조사해 보니 **이미 모으고 있었다 — 사고로.**
플랜: `~/.claude/plans/dazzling-greeting-snail.md`. 규칙 한 장: [PRIVACY.md](PRIVACY.md).

### 고치기 전에 무엇이 문제였나 → 어떻게 풀었나

| 문제 | 어디 | 풀이 |
|---|---|---|
| 모든 업로드의 파싱 전문·PDF 원본·음성 전문이 `fixtures/raw/{파일명}.*` 로 무기한 남았고 실제 사용자 발표 13건이 git 에 커밋됨 | `bridge.py` `_save_slidedoc_cache` 등 | `demo/session_archive.py` — `var/data/sessions/YYYY/MM/DD/<UTC타임스탬프>_<8hex>/`. `ls` 만으로 시간순. 13건 `git rm` (사본은 `~/chuckchuck-removed-decks/`, 히스토리 재작성은 팀 결정으로 남김) |
| 파일명이 키라 같은 이름이면 서로 덮어씀, cached-* 는 못 찾으면 **가장 최근 업로드(남의 자료)** 를 돌려줌 | `_handle_cached_slidedoc` `:515` | id 는 서버 발급, 정규식으로만 받고 경로는 id 안의 날짜로 계산. cached-* 는 id 로만, 최신본 폴백 제거 |
| `cached-takes` 가 인증 없이 전체 목록 | `bridge.py:549` | `DEMO_DEV_ROUTES=1` 일 때만 (기본 404) |
| About 의 「1년 뒤 지워요」 가 구현 없음, 동의 없음 | `app.js:8698` | 드롭존 체크박스(기본 꺼짐) → `?consent_learning=1` → manifest 고정. 동의 세션만 원본·산출물·QA턴·피드백, 1년 뒤 prune. 동의 없음 = 파싱본·받아쓰기 24h 캐시. 리포트 「이 발표 기록 지우기」 → `DELETE /api/v1/sessions/{id}` |
| 서버 형식 검사 없음 (프론트 확장자 정규식뿐) | `_handle_parse` | 매직바이트(`%PDF` / zip+`ppt/presentation.xml`) 아니면 415, 디스크에 안 씀 |
| 프론트 세션 id 가 `'flat'` 하나 | `app.js:7610` | `qaSessionId()` = 발급 id ‖ 'flat'. 모든 분석·판정 호출에 `session_id` |

### "학습" 이 실제로 무엇인가 (판단)

업로드에는 사람 라벨이 없다 — F-18 LoRA 는 AI Hub 40인 평가 라벨로 저장소 밖에서 학습했다.
그래서 순서는 (1) 실제 세션으로 **회귀 측정**, (2) 라벨 없는 **또래 기준선**, (3) 프롬프트를 고친 뒤
**재생 diff**, (4) 사람이 고친 판정이 쌓이면 그때 LoRA. 이것만 라벨이다: 👍/👎 · 「이 판정은 아닌
것 같아요」 · 「높아요/낮아요」 · 「이건 반복이 아니에요」. 침묵·모델 출력·점수는 라벨이 아니다.

### 산출물 (커밋순)

1. `ebf356c` **feat(archive)** — 보관소·브리지 배선·매직바이트·동의 UI·삭제·git rm·문서(PRIVACY·SCHEMA §10-B·DEPLOYMENT §5).
   검증: pytest 740 (+37: `test_session_archive.py` 22, `test_bridge_parse.py` 15), node smoke 30.
2. (다음 커밋) **feat(learning-loop)** — 피드백 버튼(리포트 채점표 항목 「높아요/낮아요」, 말버릇 「이건 반복이
   아니에요」, 질문 코칭 👍/👎·「이 판정은 아닌 것 같아요」; 동의 세션에만 그린다) + 오프라인 도구:
   - `examples/build_eval_bundle.py` → `exports/eval_bundles/*.json` (동의 세션 → `live_qa_run.json` 모양).
     `examples/qa_eval.py --bundle-dir exports/eval_bundles` 가 N건 평균±편차를 찍는다.
   - `chuckchuck/f22_peer_norm.py` + `examples/build_peer_norms.py` → `data/norms/percentile_table.json`.
     버킷(상황×길이)당 n≥20 미만이면 줄을 안 만들고 「아직 비교할 만큼 모이지 않았어요」.
   - `examples/replay_sessions.py --stage rubric|judge|questions` → `exports/replay/*.md` (그때 vs 지금 diff).
   - `examples/export_lora_corrections.py` → `exports/lora_corrections.jsonl` (문턱 30세션·200정정).
   검증: pytest 756, node smoke 33, 빈 데이터 디렉터리로 각 스크립트 실행.

### 팀이 따라 할 순서

```bash
sudo systemctl restart chuckchuck-bridge          # DEMO_DATA_DIR 기본 var/data (WorkingDirectory 아래)
sudo journalctl -u chuckchuck-bridge -n 5          # "세션 보관: …" · "만료 세션 정리: N건" 이 찍힌다
# 자료 올리기 → 응답에 session_id → 오늘 폴더가 시간순으로 보인다
ls var/data/sessions/$(date -u +%Y/%m/%d)/
# 학습 동의를 켠 발표가 분석까지 끝나면
python examples/build_eval_bundle.py && python examples/qa_eval.py --bundle-dir exports/eval_bundles --no-coach --limit 2
python examples/build_peer_norms.py                # n 이 찰 때까지는 빈 표
python examples/replay_sessions.py --stage judge --limit 3   # 프롬프트를 고친 뒤
```

### 남은 것

- **푸시**: 이 머신에 GitHub 자격 증명이 없어 `git push` 가 실패한다 (`gh` 도 없음). 로컬 main 이 origin 보다 앞서 있다.
- `#/replay` 화면은 `DEMO_DEV_ROUTES=1` 로만 열린다. 로컬 반복 테스트 때 `.env` 에 켠다.
- 옛 `fixtures/raw/*.transcript.json`·`*.preview.pdf`·`stage_cache/` 로컬 잔재는 브리지가 더 읽지 않는다. `rm` 해도 된다.
- git 히스토리에는 지운 발표 자료가 남아 있다. 공개 저장소로 갈 거면 `git filter-repo` — 팀 결정.
- 또래 기준 리포트 카피(「또래 상위 N%」) 연결은 버킷 n≥20 이 찬 뒤. `f22_peer_norm.describe()` 가 문장을 준다.
- LoRA 재학습은 정정 200건 뒤, 저장소 밖에서.

## 2026-09-10 — 「모르겠어요」 사다리를 근거에 못 박는다 (Claude 세션, 같은 날 2차)

"QA 세션에서 「모르겠어요」를 눌렀을 때 단계가 더 논리적이고, 전보다 구체적으로
보여 주면 좋겠다" 에서 출발했다. 같은 날 1차 세션이 만든 재료(anchor 장·정제 본문·
`examples/qa_eval.py`)를 그대로 썼다 — 먼저 지금 사다리를 실제로 눌러 보고 재고,
그 다음 고쳤다.

### 고치기 전에 무엇이 문제였나 → 어떻게 풀었나

| # | 고치기 전 (실측·코드) | 어떻게 풀었나 | 어디 |
|---|---|---|---|
| 1 | 「모르겠어요」 **두 번**이면 답이 나왔다. 스펙(§5.3)은 "간접 힌트를 단계적으로, 세 번째에 자기 말로". | 3단: **위치**(narrow) → **발판**(scaffold) → **해설**(explain). `_coach_stage` 가 같은 질문의 포기 횟수로 0/1/2+ 를 센다. | `f09._coach_stage` |
| 2 | 1단 되물음이 자료를 안 가리켰다 — 실측 `"…한 단어로 묶을 수 있나요?"`. 어느 장, 어떤 문장인지 없음. | F-08 이 질문에 **근거 장의 문장 한 줄**과 장 번호(`evidence_quote`·`evidence_slide_no`), 그 장에서 한 말(`speech_quote`)을 저장한다. 1단 react 가 "자료 3장은 이렇게 말해요: «…»" 로 시작하고, 되물음은 그 문장에 대한 **둘 중 하나**(`choices`). 장 번호가 문장에 있어 화면이 그 장 그림을 붙인다 (`hintSlideNos`). | `_evidence.quote_for` · `f08._evidence_quote` · `f09._narrow_followup` |
| 3 | 선택형 되물음은 프롬프트 부탁뿐이라 LLM 이 자주 넓게 물었다. | **코드 검증**: `choices` 2개 + 선택형 정규식이 아니면 골자에서 만든 결정적 문장("이 장이 말하는 건 A 쪽인가요, B 쪽인가요?")으로 폴백. 인용이 없는 옛 질문은 예전 그대로. | `f09._narrow_followup` |
| 4 | 2단이 없었다. "질문을 좁힌다" 는 말만 있고 답할 **재료**를 안 줬다. | **빈칸 발판** — 골자에서 명사 하나를 가린 문장 + 선택지(정답 · 이웃 개념 요약에서 뽑은 오답). **LLM 0회**, 항상 같은 결과. 처음엔 "때문입니다"·"흔들린다." 같은 서술어를 가렸다 → 어미·조사를 떼고 명사 줄기만, 동률이면 뒤쪽(결론 쪽) 낱말. | `_evidence.mask_gist` · `f09._scaffold_judgement` |
| 5 | 해설이 **출처를 안 댔다**. | 해설 끝에 코드가 `— 자료 N장: «인용» 발표에서는 «한 말» 라고 말했어요.` 를 반드시 단다 (500자 안에서). | `f09._with_citation` |
| 6 | 포기한 사람에게 코치가 "지금 핵심을 잘 짚으셨어요" (프롬프트가 금지해도 A.X 가 무시, 높임 '~셨' 까지). | 칭찬·높임 정규식에 걸리면 결정적 문구("괜찮아요. 여기서 같이 짚어 볼게요.")로 바꾼다. | `f09._COACH_PRAISE_RE` |
| 7 | 「힌트 보기」 버튼과 코칭이 **사다리가 둘**이었다 (내용·출처 다름). | 힌트 사다리 첫 칸이 같은 자료 인용, 발판 칸이 같은 빈칸 — 한 소스(`Question` 의 인용 필드). 인용이 없는 옛 질문은 사다리 길이가 안 바뀐다 (기존 테스트 `len(hints)==3` 유지). | `f08._hint_locate` · `_hint_scaffold` |
| 8 | 진행 표시가 없었다 (버튼 라벨만). | 코치 말풍선 머리말 「막힘 1/3 · 자료에서 같이 찾아요」, 자료 인용 **카드**(장 그림 포함), 되물음의 **선택 칩**(누르면 답칸에 들어가고 바로 보내지 않음), 「모르겠어요」 라벨 3단. 옛 세션 턴은 예전 렌더. | `qa_live.js` · `app.js streamRow` · `app.css` |

### 산출물 (커밋순)

**549c228 — [그래프] 인용·빈칸·해설 출처 3단 + [코드 가드]**
- 계약: `Question` 에 `evidence_slide_no`·`evidence_quote`·`speech_quote`, `QaJudgement` 에
  `choices`·`evidence_quote`·`evidence_slide_no`, `QA_COACH_STAGES` 에 `scaffold`. 전부
  optional 기본값 — 브리지·서버·프론트·옛 세션이 그대로 돈다. `SCHEMA.md §8-E·8-F` 갱신.
- 하네스에 코칭 지표: 단계 순서 · 1단 장 번호 · 선택형 · 인용∈자료 · 해설 출처 · 높임.
  `SLIDE_NO_RE` 가 `S8` 꼴도 센다 (판정 본문 덕에 해설이 이미 그 꼴로 출처를 대고 있었다).
- 검증: 703 passed · 7 skipped. 새 테스트 — `test_qa_evidence.py` 14건(인용·빈칸·F-08 저장·
  사다리·3단·가드·직렬화), `test_stuck_coaching.py` 를 3단 규칙으로 (두 번째 포기 → 발판, LLM 0회).
- 측정 (같은 픽스처 · A.X · `exports/qa_eval/…coach-*.json`):

  | | 고치기 전 | 고친 뒤 |
  |---|---|---|
  | 단계 | narrow→explain | narrow→scaffold→explain (3/3) |
  | 1단 장 번호 | 0/3 | **3/3** |
  | 1단 선택형 | 0/3 | **3/3** |
  | 1단 인용∈자료 | - | **3/3** |
  | 해설 출처 | 0/3 | **3/3** |
  | 코칭 높임 | 3 | **0** |
  | LLM 호출/질문 | 2 | 2 (발판은 0회) |

  실측 예 — 1단: `자료 1장은 알림이 '시간을 훔치는 게 아니라 머릿속 흐름을 끊는다'고 말합니다.
  이 말은 알림 확인 시간보다 작업 맥락 복구…` / 2단: `빈칸을 채워 보세요: 알림 확인 시간보다
  작업 맥락 복구에 드는 인지 ___ 더 크다. — '비가시적' 인가요, '비용' 인가요?` / 3단: 해설 +
  `— 자료 1장: «…» 발표에서는 «…» 라고 말했어요.`

**bc65b7a — [화면] 단계 머리말 · 인용 카드 · 선택 칩 · 라벨 3단**
- `qa_live.js`: 코치 응답의 `coach_stage`·`evidence_quote`·`evidence_slide_no`·`choices` 를 턴에 싣는다.
  서버가 1단 react 뒤에 붙인 인용은 카드로 그리므로 본문에서 뗀다(`coachReactText`). 선택 칩은
  스트림이 말풍선마다 innerHTML 을 갈아 끼워서 문서에 한 번만 위임한다.
- `app.js streamRow`: 코치 턴 머리말 4종, `.qa-evidence` 카드 + 장 그림(힌트와 같은 방식),
  되물음의 `.qa-choice-chip`. `choices`·`quote` 가 없는 옛 세션 턴은 예전 그대로 (stub 렌더로 확인).
- `index.html ?v=` 세 파일 `ql1` (어느 계보에서도 안 쓴 값). node 스모크 33 passed (순수 함수 3건 추가).
- 브라우저 실물 확인은 못 했다 (이 머신에 chrome-devtools MCP 연결 실패). `streamRow` 를 stub 으로
  돌려 마크업이 만들어지는 것까지만 봤다 — **리허설 때 눈으로 봐야 한다.**

### 남은 것

- 골자에 영문 용어가 있으면 빈칸이 `___ residue` 처럼 잡힌다 (실측 1건). 영문은 후보에서 빼거나
  두 낱말 묶음을 가리는 규칙이 필요하다.
- 하네스가 이번 실행에서 잡은 별건: 질문 반말 3/3(LLM 편차), 함정정정→통과 0/1(1회 편차).
  코칭과 무관하며 F-08 어미 후처리가 답이다 (1차 세션 「남은 것」 그대로).
- 푸시는 여전히 막혀 있다 (`Authentication failed`). 로컬 커밋 8개: 0d89719·e20c05f·bf05592·29e456f·549c228·6b406fe·bc65b7a·(이 문서).

## 2026-09-10 — 질문 코칭 LLM 컨텍스트 재설계: 하네스 + 그래프 엔지니어링 (Claude 세션)

"AI 세팅을 제대로 못 해서 대답이 아쉽다 — 그래프 엔지니어링 같은 걸 쓸 수 없나" 에서
출발했다. 코드를 읽기 전에 **돌고 있는 브리지에 실제로 붙여 봤고**, 그 실측이 방향을
정했다: 문제는 프롬프트 문구가 아니라 **LLM 에 실리는 자료**였다.

- 개념당 자료 본문 400자 중 **70% 가 Upstage 이미지 캡션·HTML** (`<figcaption>…A well-lit desk…`)
- F-07 이 핵심 개념에 근거 장을 **12장 전부** 붙여서, 세 개념이 똑같은 덱 첫 장 400자를 받았다
- **판정(F-09)은 자료 본문을 0자** 받았다 — summary 한 줄과 발화 300자로 "자료와 어긋나는가" 를 판정
- 그래프는 "경로=A > B · 연결=C, D" **이름 한 줄**로만 쓰였다

그래서 두 축으로 나눴다. **[하네스]** 는 자 — 고치기 전후를 같은 표로 재는 도구.
**[그래프]** 는 본체 — 이미 있는 개념 그래프를 장식이 아니라 **근거 검색 구조**로 쓰는 것.
둘은 대체 관계가 아니다: 자 없이 그래프를 넣으면 "좋아진 것 같다" 에서 못 벗어나고,
실제로 이 세션에서 하네스가 그래프 작업의 퇴행 하나를 잡았다 (아래 e20c05f).

### 계획 대비 진행

| 단계 | 축 | 내용 | 상태 |
|---|---|---|---|
| 0 | 하네스 | `examples/qa_eval.py` — 인용률·특이도·폴백·판정 일관성·말투·프롬프트 잡음 | **완료** (0d89719) |
| 1 | 그래프 | `_evidence.clean_slide_text` — 캡션·HTML 제거, 잡음 76% → 0 | **완료** (e20c05f) |
| 2 | 그래프 | `anchor_slides` — 12장 개념을 본문이 그 개념을 말하는 3장으로 | **완료** (e20c05f) |
| 3 | 그래프 | 서브그래프 번들 — 이웃 요약·간선 종류·구간 + anchor 본문·발화를 F-08·F-09 에 | **완료** (e20c05f) |
| 4 | 그래프 | `evidence_quote` — 질문이 인용한 구절을 코드가 자료에서 검증, 함정은 `false_premise`/`true_fact` | 안 함 — 지표 보고 결정하기로 |
| 5 | 판정 규칙을 코드로 | 무관 가드·함정 가드·「모르겠어요」 순서·조사/경어 | **완료, 앞당김** (e20c05f) — 3단계 측정이 요구했다 |
| 6 | 하네스 | solar-pro3 vs A.X-K1 A/B (같은 표) | 대기 — Upstage 키 미발급 |

### 산출물 (커밋순)

**5906a0f — Cloudflare Tunnel 로 데모를 여는 스크립트** (질문 코칭과 별개, 같은 세션)
- `demo/run_tunnel.sh`. 브리지는 인증이 없어 `DEMO_HOST=0.0.0.0` 이 금지인데, 원격
  시연 요구가 있었다. 브리지를 127.0.0.1 에 둔 채 터널만 뚫고 **Access 를 반드시**
  건다. 임시 주소(trycloudflare)는 인증이 없어 거부한다.
- 검증: 브리지 없음·주소 없음 가드 둘 다 실행. cloudflared 2026.9.0 설치. 계정
  로그인(도메인 필요)은 사람 몫이라 여기서 멈췄다.

**0d89719 — [하네스] 측정 도구와 본문 정제 헬퍼** 
- `examples/qa_eval.py`: `fixtures/live_qa_run.json` 한 벌(자료·발화·그래프·정합·심사)로
  F-08·F-09 만 실 LLM 을 불러 표를 남긴다. `RecordingLLM` 이 프롬프트·응답·지연·잡음을
  기록한다. 결과는 `exports/qa_eval/<시각>_<tag>.json`.
- 재는 것: **근거 인용률**(모범답 낱말이 자료·발화에 있는 비율) · **질문 특이도**(이 자료
  고유 낱말 수) · **폴백 비율** · **판정 일관성**(골자→통과 / 엉뚱→wrong / 함정동의→wrong /
  함정정정→통과) · **말투 위반**(금지 높임, 반말 질문) · **프롬프트 글자·잡음·지연**.
- `_evidence.clean_slide_text` 는 이 커밋에서 **일부러 배선하지 않았다** — 베이스라인은
  지금 코드 그대로 잰 것이어야 한다.
- 검증: `tests/test_evidence.py` 7건. 베이스라인: 인용률 0.42 · 엉뚱→wrong 2/3 ·
  함정동의→wrong 0/1 · F-08 프롬프트 잡음 14% · 판정 프롬프트 936자(본문 0).

**e20c05f — [그래프] 질문·판정이 자료를 보게 한다 + [코드 가드]**
- `chuckchuck/_evidence.py` (유틸, `_match.py` 와 같은 자리 — DEV_POLICY §4-1 은 F-모듈끼리를
  말하므로 F-08·F-09 가 같이 import 해도 위반이 아니다):
  `clean_slide_text` · `anchor_slides`(label·summary 낱말 겹침으로 F-07 slide_nos 안에서 top-3)
  · `neighbor_lines`(상위/하위/관련 + 요약 + anchor 장) · `section_line`.
- F-08: 개념마다 anchor 장 본문(정제, 400 → 1200자)·발화(300 → 500자)·이웃 상세·구간.
  `Question.slide_nos` 가 anchor 라 힌트·모범답·화면의 장 그림·판정 본문이 **같은 장**을 본다.
  폴백 골자의 "(1, 2, …, 12장 근거)" → "(2, 3, 4장 외 9장 근거)".
- F-09: `slidedoc` 인자 신설, "## 자료 근거 장 본문" 블록. 브리지는 F-08 과 같은 디스크
  캐시에서, 서버는 세션 아티팩트 `SLIDE_DOC` 에서 넘긴다. 안 주면 예전 그대로.
- 코드 가드 (프롬프트로 부탁만 하던 규칙을 코드가 받는다 — `_enforce_good` 과 같은 규율):
  `_enforce_on_topic` 질문·자료와 낱말이 하나도 안 겹치는 답 → wrong ≤35 (근거 낱말 30개
  미만이면 안 건다 — 얇은 근거에서 "안 겹침" 은 신호가 아니다).
  `_enforce_trap` 함정 질문에 `premise_corrected=false` → wrong ≤35. 값이 없으면 안 건드린다.
  「모르겠어요」 버튼이 빈 답 검사보다 먼저 간다 (주석은 "버튼이 먼저" 였는데 코드가 반대였다).
  `_FOLLOWUP_*` 의 띄어 붙은 조사("인지 자원 를")·`~시겠어요` 제거. 코치 프롬프트에 빈말 칭찬 금지.
- **하네스가 잡은 퇴행**: 3단계만 넣고 재니 `엉뚱→wrong` 이 2/3 → **1/3** 으로 나빠졌다.
  물류 창고 이야기에 partial 75, react 는 "스마트폰 시야 밖 두기까지는 정확합니다" —
  새로 실은 **자료 본문을 발표자의 답으로 착각**한 것. 자료를 더 실을수록 커지는 착각이라
  규칙 1-1("답변에 있는 것만 답변이다") + 무관 가드로 막았다. 자가 없었으면 못 봤다.
- 검증: `tests/test_qa_evidence.py` 22건 (anchor·이웃·판정 본문·가드·순서·문구), 전체
  **682 passed · 7 skipped**. 측정 (같은 픽스처·같은 A.X):

  | | baseline | 3단계 | 5단계 |
  |---|---|---|---|
  | 근거 장 평균 | 7.0 | 3.0 | 3.0 |
  | 프롬프트 잡음 | 14% | 2% | 2% |
  | 근거 인용률 | 0.42 | 0.45 | **0.58** |
  | 골자→통과 | 2/3 | 3/3 | 3/3 |
  | 엉뚱→wrong | 2/3 | **1/3** | **3/3** |
  | 함정동의→wrong | 0/1 | 0/1 | **1/1** |
  | 판정 프롬프트 | 936자 | 1299자 | 1324자 |

### 남은 것

- **푸시 안 됨** — `Authentication failed` (credential helper·gh 없음). `0d89719`·`e20c05f` 가
  로컬에만 있다. `git push origin main` 을 사람이 해야 한다.
- **질문이 반말** — 세 질문 다 "…되는가?" 꼴. f08 규칙 5(해요체)를 A.X 가 무시한다.
  하네스에 `반말 질문 N/3` 지표를 넣어 뒀다. 다음은 코드 후처리(어미 교정)가 맞다.
- 4단계(인용 검증)·6단계(A/B)는 위 표대로 미착수.
- `app.js:2742` "끝까지 하셨어요" · `data.js:297,319` 샘플 카피의 높임 — JS 라 `?v=` 갱신 필요.
- F-18 LoRA: torch 2.14+cu130 은 깔았지만 **어댑터·베이스 모델(22GB)이 이 머신에 없다.**
- F-01 신규 업로드: Upstage 키 미발급. 픽스처 뒤쪽 파이프라인은 전부 돈다.

### 운영 메모

- 이 머신(`/home/yehschuck/project/chuckchuck`)은 CLAUDE.md 가 가정한 데모 머신이 아니다.
  로컬 실행: `sudo apt-get install python3-venv python3-pip nodejs` → `python3 -m venv .venv`
  → `.venv/bin/pip install -r requirements.txt` → `HABIT_PROVIDER=heuristic .venv/bin/python -m demo.bridge`.
  LoRA 없이 heuristic 을 **명시**한다 — 그냥 두면 조용히 떨어진다.
- `.env` 는 IDE 가 열어 둔 옛 버퍼를 저장하면서 **한 번 덮어써졌다.** 복구했다.
  `.env` 탭이 열려 있으면 저장하지 말고 닫을 것. `config.load_dotenv` 는 `setdefault` 라
  셸에 이미 있는 값이 이긴다.
- `REASONING_BACKEND=ax` 로 둔 이유: solar 는 키가 없고, exaone(Friendli)은 생각 과정을
  그대로 출력해 JSON 프롬프트가 깨진다 (실측). Upstage 키가 오면 solar 로 되돌리고 6단계.
- 하네스 재실행: `.venv/bin/python examples/qa_eval.py --tag <이름>` (실 LLM, 약 1.5분, F-08 1회 + F-09 9회).
  `--no-judge` 면 질문만. `--dump` 로 프롬프트 원문.

## 2026-08-09 — Q&A 코칭 최종 점검 (2026-08-08 시작, Claude 세션)

"Q&A 최종 점검을 하자"에서 출발했다. 지목된 것은 다섯 — 새 발표 시작 시 세션
초기화, 이전 QA 값이 남는 게 맞는지, 정답 판정이 너무 광범위한 것, 힌트, 마이크·
입력. 진행 중에 "자료와 다른 녹음을 올리면 아예 인식을 못 한다" 가 추가됐다.

**이 세션의 성격: 고친 것보다 「고치려다 측정에 막힌 것」이 더 많다.** 가설 셋이
숫자 앞에서 뒤집혔고, 그때마다 프롬프트·임계값을 만지는 대신 계획서를 고쳤다.

### 산출물 (커밋순)

**2b2e516 — 측정에 쓸 slidedoc fixture 고정**
- Upstage 재파싱 지터로 디스크와 git 이 어긋나 있었다. 판정을 재기 전에 바닥을
  고정하지 않으면 나중에 숫자를 되짚을 근거가 사라진다. 33장·8장 구조는 그대로.

**2261a14 — 판정이 무른지 재는 도구를 만들고, 원인이 F-09 가 아님을 확인**
- `examples/qa_judge_probe.py`. 실 LLM(solar)으로 질문 3개 × 답 5벌 = 18 판정.
- **결과가 가설을 뒤집었다.** 무관한 답 → wrong 31.7점, 얕은 답 → partial 75 로
  안 닫힘, 기대 골자 → good 81.7. `qa_mastered` 게이트가 설계대로 돈다.
  → **`QA_PASS_SCORE` 도 f09 규칙 8 대역도 건드리지 않았다.**
- 진짜 원인은 F-08 질문 중복이고, **5분 트랙에서만** 생긴다 (후보 13개 중 서로
  답이 겹치는 상위 3개만 뽑히기 때문). 10분 기본 트랙은 0.40 넘게 겹치는 쌍이
  21개 중 1개. → 코드 변경 없이 **시연을 10분 트랙으로** 하는 것이 답이다.

**ab4133a — 녹음이 자료와 다르면 구간 추정을 버리고, 왜 못 맞췄는지 화면에 말한다**
- 저장된 fixture 에서 재현했다: `MIN_CONFIDENCE 0.12` 를 0.19 로 겨우 넘겨
  "추정 성공" 이 되고, 2·3·4장이 폭 0 구간으로 남아 발화가 통째로 비었다.
  그게 "인식을 못 한다" 의 실체였다.
- `MAX_EMPTY_RATIO = 0.25` 가드 추가. **`MIN_CONFIDENCE` 는 일부러 안 올렸다** —
  올리면 멀쩡한 추정까지 떨어진다. `marks_reason` 을 화면에 정직하게 싣는다.

**5c1ca6d — 확실한 버그 셋**
- **마이크**: `hasLiveDictation()` 이 생성자 존재만 봐서, 받아쓰기가 망 오류로
  한 번 죽으면 **다시 눌러도 같은 죽은 경로로 들어갔다.** `liveDictationDead`
  세션 플래그를 세우고 그 뒤로는 녹음 폴백으로 내려간다.
- **새 발표 = 새 QA**: `resetQa()` 호출 지점 6곳 중 **업로드 경로에 없었다.**
  `docKey`(파일명+장수) 지문을 `qaLiveActive()` 가 보게 하고, `applySlideDoc()`
  에서도 지운다. 옛 세션의 `undefined` 는 낡음으로 안 쳐서 새로고침에 안 날린다.
- **말풍선 중복**: `revealHalf` 와 `askAgain` 이 같은 `missing_points` 를 연달아
  밀어 넣었다. `revealHalf` 가 펼쳤는지 반환하게 하고 `askAgain` 이 건너뛴다.

**312fe06 — 발표 중 「처음부터」가 녹음기를 놓아주지 않았다**
- `ccRuntime` 을 안 끝내서 화면만 처음으로 가고 녹음은 계속 돌았다.
  `stopLiveRehearsal()` 을 만들어 리셋 세 경로에서 부른다.

**3fd745a · 988247e — 우리가 추론한 개념 배치를 발표자가 고른 것처럼 묻던 것**
- 제보: 수면 발표인데 "왜 다른 **정렬 방식** 대신 이 방식을 선택했나요" 가 나왔고,
  모범답이 "**경로(위계)에서** 수면의 질 > 회복으로 표시되어 있습니다" 였다.
  발표자는 그 배치를 **본 적이 없다** — 우리 프롬프트 발판을 자료인 척 인용한 것.
- 프롬프트로 한 번(3fd745a), 그다음 **결정적 가드로** 한 번 더(988247e).
  `_cites_scaffold()` 는 **낱말이 아니라 인용 꼴**(`경로(위계)`·`연결=`·`위계로 표시`)
  로 잡아서, 자료가 진짜 쓰는 "정렬 방식"(IMU2CLIP)·"최단 경로"는 살아남는다.
  테스트로 고정했다.
- **여기서도 설계 하나가 측정에 기각됐다.** 모범답을 자료와 대조해 겹침이 낮으면
  버리려 했으나 신호가 거꾸로였다: 좋은 답 9%·10% 대 살붙인 답 24%, 발판 유출은
  0%. F-08 이 `SlideDoc` 을 안 받고 개념 요약(토큰 9개)만 받기 때문이다.
  → 바닥값을 넣지 않았다. **「뇌파 측정을 통해」류 살붙임은 아직 못 막는다.**

**ad6de9d — 자료와 무관한 녹음이면 「아예 다른 내용」이라고 판정해서 말한다**
- "잘 맞지 않아요" 는 구간을 못 맞췄다는 말이지 **왜** 못 맞췄는지가 아니다.
  다른 파일을 올린 사람은 그 문구로 원인을 못 찾는다.
- 정렬과 무관한 전역 신호 둘(IDF 가중 어휘 겹침 · 발화 창의 슬라이드 커버율)이
  **둘 다** 임계 아래일 때만 단정한다. 임계 0.2/0.6 은 실녹음 2종을 자료 6종에
  교차로 붙인 실측으로 정했다. 한국어끼리 흔한 낱말이 겹치는 경계 사례(RINGLE
  0.258)는 **단정하지 않는다** — 확신 없는 단정이 더 나쁘다.
- `marks_match` 로 리포트 맨 위 경고까지 올린다. 저장 리포트 복원 경로에도 남긴다.
- 검증: 수면 녹음을 자료 9종에 붙여 맞는 짝 2종은 통과(0.671), 불일치 6종은 전부
  `unrelated`. focus 자료는 conf 0.227 로 **옛 가드를 통과하던 값**이라 특히 중요.

**a13f989 · 65334df — 마감 정산과 근거 정정**
- 계획서에 "일부러 안 한 것" 을 이유와 함께 남겼다. CLAUDE.md 회귀 기준선이
  552(§3-5)·498(§4)로 서로 다르고 둘 다 낡아서 실측 **591** 로 맞췄다.
- **65334df 는 내가 어제 적은 근거를 뒤집은 커밋이다.** W4-b(출구 조건)를 유지하는
  이유로 "코드 주석이 의도를 기록하고 있다" 를 댔는데 틀렸다 — 그 주석의 의도는
  조건을 AND→OR 로 바꾼 것이지 `<=` 가 아니고, `<` 로 바꿔도 힌트 소진 분기는
  살아 있어 옛 버그는 안 돌아온다. 결론이 맞아도 근거가 틀리면 결론을 못 지킨다.

### 일부러 안 한 것

- **W4-b 출구 조건**: 유지. 진짜 근거는 **출구를 눌러도 리포트가 안 부푼다**는 것.
  `closeRetell` 이 답을 본 뒤의 발화를 판정하지 않고, `revealed` 는 `bucketOf`·
  연속 기록·방어 집계·리포트 `skipped` 네 곳에서 일관되게 빠진다. **답을 보고
  통과하는 길이 구조적으로 없다** → "판정이 광범위한 체감의 절반이 여기일 수
  있다"는 W3 가설도 같이 기각. 그리고 `<=` 가 `liveStalled` 라는 이름에 맞는다 —
  점수가 제자리인 것이 곧 막힌 것이고, 좁히면 정작 막힌 사람이 출구를 잃는다.
- **W4-a 힌트 장 번호 구조 전송**: 전제는 맞지만 프론트 정규식이 서버가 실제로
  쓰는 문장 형식 전부에서 맞는 답을 낸다. 마감일에 양쪽을 건드릴 값어치가 없다.
- **W4-c 힌트 분모 흔들림**: 실재하나 미용. 말풍선에 `total` 을 싣게 고친 뒤로
  거짓 숫자("4/3")는 안 뜬다. 고칠 자리는 계획서에 적었다.
- **W5-b 입력 카드 버튼 6개**: MVP_SPEC §3 위반 맞으나 마감일 레이아웃 수정 보류.
- **W2(c) 서버 세션 키 분리**: `liveArtifacts()` 가 다섯 키를 늘 보내므로 오늘
  새는 데가 없다. 마감 뒤 보험.

### 검증

- `python -m pytest tests/ -q` → **591 passed · 7 skipped** (세션 시작 581).
  새 테스트 10개: 구간 추정 가드 3 · 발판 인용 차단 3 · `match` 4단 상태 4.
- 브라우저 확인은 사용자가 수행("거의 다 고쳐졌어"). **`tests/` 23개가 전부
  파이썬이라 프론트 JS 는 자동 커버리지가 0이다** — 세션 초기화·마이크 폴백·
  「처음부터」·말풍선 중복은 원리상 눌러야만 확인된다.
- 코드로 확인한 것: Enter 입력이 `shiftKey`(줄바꿈)와 **`isComposing`/`keyCode 229`
  (한글 조합 중)** 를 거른다. 힌트 맥락이 `hintsShown` → 어댑터 스네이크 변환 →
  `bridge.py` → `f09` 4단으로 무결하게 전달된다 (카멜↔스네이크 유실 없음).

### 운영에서 걸린 것 (다음에 시간 날리지 않게)

- **브리지는 파이썬을 메모리에 올린 채 돈다.** 17:15 에 뜬 프로세스로 18:44~20:40
  커밋 셋을 검증하려 했는데 **하나도 반영돼 있지 않았다.** JS 는 매 요청 디스크에서
  읽어 즉시 반영되지만 파이썬은 아니다 → **파이썬을 고쳤으면 재기동 후 예열한다.**
- `pkill -f "python -m demo.bridge"` 는 **자기 셸까지 죽인다** (명령줄이 패턴과
  일치). keepalive 도 같이 날아가서 손으로 다시 세웠다.
- keepalive 가 두 개 돌고 있었다. 둘이 동시에 재기동하면 서로 죽인다 — 하나만 남긴다.

### 이어서 — 「일부러 안 한 것」 되짚기 (2026-08-10 새벽)

위 정산에서 미룬 것들을 백로그([qa-followups.plan.md](../.claude/plans/qa-followups.plan.md))
로 남기고 순서대로 열었다. **이번에도 측정이 계획서를 고쳤다.**

**dac6239 — 입력 카드의 버튼 여섯을 두 줄로 나눈다** (위 W5-b 해제)
- 주 행동 둘(보내기·마이크)만 한 줄에 두고 나머지를 보조 줄로 내렸다. 버튼은
  하나도 안 없앴다 — `liveReveal` 은 판정 실패 시 유일한 탈출구이고 「여기까지」는
  다크패턴 방지상 항상 있어야 한다.
- **두 줄 다 `step-actions` 클래스를 유지해야 한다.** 판정 중 잠금이
  `$('.qa-live-input .step-actions button')` 로 걸려 있어서, 보조 줄에서 이 클래스를
  빼면 판정 중에도 눌린다.

**efefcfe — 골자가 사실상 같은 질문을 걸러낸다** (위 W3 의 진짜 원인)
- 5분 트랙에서 세 질문의 모범답이 사실상 같아 "아무 답이나 맞는 것처럼" 보이던 것.
  `answer_gist` 토큰 겹침으로 결정적으로(LLM 없이) 걸러낸다.
- **백로그에 적어 둔 임계값 0.40 을 그대로 못 썼다.** 그 숫자는 측정 도구의
  토크나이저에서 나온 것이라 여기 토크나이저로는 **진짜 쌍둥이 셋 중 둘(0.24·0.26)을
  놓쳤다.** 직접 재서 0.20 으로 잡았다 (쌍둥이 0.24~0.43 대 다른 골자 0.00~0.04).
- **질문 개수를 절대 안 줄인다.** 여유분을 짧은 트랙에만 줘서 10분 기본 트랙은
  되채우기로 결과가 종전과 동일하다 — 시연 경로는 구조적으로 무영향.

**a93a974 — 브라우저 없이 도는 프론트 스모크** (위 「검증」의 *JS 커버리지 0* 해제)
- `qa_live.js` 는 최상위에 부작용이 없어 `vm` 컨텍스트에 통째로 올린다. `app.js` 는
  최상위에서 `route()` 가 돌아 못 올리므로 `qaDocKey` 하나만 이름으로 잘라 쓴다.
  못 찾으면 폴백 없이 던진다 — 손으로 베낀 사본을 두면 그때부터 원본이 아니라
  사본을 시험하게 된다.
- **전역 스텁이 곧 시험 범위다.** `hasRealSlideImage`·`qaDocKey` 는 원본이
  `typeof … === 'function'` 으로 감싸고 있어 안 심으면 그 가지가 죽은 코드가 된다.
- **마지막 케이스가 하네스 자신을 검사한다** — 고치기 전 `liveHints()` 를 소스
  문자열 위에서 되돌려 같은 단언이 반드시 깨지는 것을 확인한다.

**eb968a8 — 세션 아티팩트 키 계약** (위 W2(c) 해제)
- 세션 키를 나누려다 전제를 먼저 쟀더니, "새는 데 없다" 가 참일 뿐 아니라 **설계로**
  지켜지고 있었다. `put_artifacts` 는 명시적 `None` 을 지우기로 계약했고 docstring 이
  사건까지 기록해 뒀다.
- 그래서 키를 나누는 대신 **계약이 깨지는 유일한 조건**을 잠갔다 — `ARTIFACT_KEYS` 에
  여섯 번째 키가 늘고 `liveArtifacts()` 가 안 따라오는 경우.
- 키를 안 나눈 이유: 지금 새는 데가 없는데 바꾸면 브리지의 기존 아티팩트 조회가
  안 닿는다. 마감일에 관측되는 이득 없이 위험만 든다.

**아직 안 한 것 — 백로그 4번(SlideDoc → F-08).** 값어치는 제일 크지만 F-08 프롬프트에
슬라이드 본문을 넣는 변경이라 모든 발표에서 토큰·지연·과금이 늘고, 검증에 실 LLM 을
여러 번 돌려야 한다. 실패하면 질문 생성 자체가 망가진다 — 마감 뒤 과제.

**검증**: `node tests/js/qa_live.smoke.mjs` 25 케이스 · `python -m pytest tests/ -q`
**597 passed · 7 skipped**. 브라우저 클릭은 입력 카드 두 줄(`?v=qh3`)이 남아 있다.

## 2026-08-09 — 리포트 정보 다이어트 + 탭 전환 최적화 + QA→리포트 E2E 점검 (Claude 세션)

"마지막 리포트가 정보의 바다"라는 관찰에서 출발. 요약 탭을 열면 여덟 덩어리
(판정 헤드·복습 카드·트로피·덱·개념 판정·코칭 내역·보완 3·CTA)가 동시에 말을
걸어 결론이 안 읽혔다. 방향 합의: **덱은 데모의 주인공이라 펼쳐 두고, 나머지를
예고형 접기로** — 전역 「핵심만 보기」 모드는 마감일 회귀 위험이 커서 버렸다.

**5270c02 — 요약 탭 2층 구조 · switchReportTab · E2E 버그 2건**

- 정보 다이어트: 기본 노출은 결론 층(판정 헤드·오늘 가져갈 문장·슬라이드 덱·
  보완 1가지)만. 코칭 내역·복습 한 마디는 접기로 내리되, 접힌 줄이 열 가치를
  먼저 말한다 — 「교수님과 주고받은 3개 질문 · 대화로 2개 늘었어요」,
  「11개 · 다시 볼 곳 3개」 + 다시 볼 곳이 있을 때만 빨간 점(유도).
  기존 judge-fold 관례 재사용, 새 문법 발명 없음.
- 탭 전환 최적화: 탭마다 renderReport() 전체를 다시 불러 왼쪽 판정 헤드
  (완성도 점수·차원 막대)가 재렌더·재애니메이션됐다. 본문(#rbody)만 갈아끼우는
  switchReportTab 으로 분리 — async 렌더 직후 DOM 을 짚던 goRubric·goJudge 의
  경주도 같이 사라졌다(이제 동기).
- E2E 점검(QA 코칭 기록 → 리포트 → 연습 도구) 중 잡은 둘:
  ① recordQaHistory 실전 경로가 「질문 전/후」를 샘플(3/5)에서 베껴 적었다 —
  질문 4개짜리 코칭에 「4개 중 5개 늘었어요」가 가능했다. 실기록에서 센다:
  질문 후 = 최종 설명해낸 것(full), 질문 전 = 그중 힌트·재시도 없이 첫 답에
  설명한 것. ② #/report/last·sample-imu2clip 주소를 그대로 QaHistory 키로
  조회해 코칭 내역이 소리 없이 사라졌다 — last 는 최근 기록, 샘플은 목
  키(imu2clip)로 해석. 연습 도구는 이미 실데이터 도출 + 정직한 빈 상태가
  있어 손댈 것 없음.
- 검증: pytest 591 초록 · node --check · Playwright 헤드리스로 DOM 검사
  11항목(헤드 노드 유지 왕복·접기 기본 닫힘·메타 문구·last 해석) + 콘솔 에러
  없음 + 스크린샷 육안. app.js ?v=qg6.
- **남은 것**: 스펙 §5.4의 「발표에서는 언급만 함 → 대화로 이해함」 변화가
  개념 판정 트리에 아직 반영 안 됨 — 판정은 파이프라인 결과만 보고 QA 결과는
  코칭 내역 접기에만 있다. 마감일이라 안 건드림. 실데이터 리포트(브리지) 육안
  확인도 리허설 때 할 것.

## 2026-08-07 — 상세리포트 「논리 흐름·음성 습관」 탭 재편 (Claude 세션)

"두 탭이 별로 와닿지 않는다"는 관찰(본인+팀원)에서 출발했다. 판정 카드·막대·
숫자는 나열돼 있는데 부스 심사위원이 30초 훑을 때 "가장 큰 문제가 뭐고 뭘
하라는 건지"가 안 읽혔다. PRD → 계획 → 구현 순서로 진행.

### 산출물 (커밋순)

**1ec83a5 — PRD: 두 탭을 누구 기준으로 왜 고치는지 확정**
- 1차 독자를 부스 심사위원·관람객으로, 성공 기준을 「처음 보는 사람이 3초 안에
  문제+처방을 자기 말로 재현」으로 못박았다. 파이프라인 변경은 범위 밖.
- [.claude/prds/report-tabs-resonance.prd.md](../.claude/prds/report-tabs-resonance.prd.md)

**4b71717 — 두 탭에 진단→근거→처방 구조 (마일스톤 1~3 구현)**
- 탭 첫 화면에 「가장 큰 문제 한 줄 + 처방 한 줄」 진단 블록(`.tab-verdict`).
  우선순위 규칙: 논리 흐름은 연결 멘트 없음 > 근거 점프, 음성 습관은 핵심 장
  시간 부족·초과 > 속도 > 간투어. 1순위 근거 카드만 펼치고 나머지는 fold 로
  접는다 (rSummary 의 기존 관례 재사용 — 새 문법 발명 없음).
- 남는 숫자 전부에 판정 말을 붙였다: 일치도 % → 「자료 순서를 잘 따랐어요」,
  자/분 → 「권장보다 조금 빨라요」, 쓴 시간 → 「목표보다 N분 길었어요」.
  숫자와 판정 색 5종은 불변 (§14 숫자는 신성하다).
- 정직성: 근거가 없으면 헤드라인을 지어내지 않고 블록째 생략. 실데이터·샘플
  경로 모두 같은 구조, 샘플 배지 유지. 기존 voice-tip 처방 문구는 진단 블록으로
  통합해 중복 제거. 파이프라인·스키마는 손대지 않았다.
- 계획: [.claude/plans/report-tabs-resonance.plan.md](../.claude/plans/report-tabs-resonance.plan.md)
- 검증: node 스모크 11케이스(우선순위·빈 데이터·이슈 없음·조사 을/를),
  `node --check`, pytest 562 초록. **남은 것**: 아이패드 가로 첫 화면 실측과
  브리지 실데이터 육안 확인 — 리포트를 열면 객석 수다 프리페치가 실 API 를
  물어 과금되므로 자동으로 열지 않았다. 리허설 때 확인 후 마일스톤 완료 처리.

**bbbf6c9 — 구성 제안(F-20)에 「왜 바꿔야 하는가」 근거 층**
- "발표 형식 의논(잡스식 등)의 피드백이 안 와닿는다"는 제보. 통보 형식(1안
  확정)은 유지하기로 하고, 순서표(뭘 하라)에 빠져 있던 효과(왜 좋은가)를 채웠다.
- ① LLM 이 gains(claim+evidence) 2~3개를 내되 evidence 가 입력의 실제 개념·구간
  이름·시각을 못 짚으면 서버가 버린다 — keep.quote 와 같은 규율. ② 순서표
  구간마다 「지금 4:10 → 제안 1:30」 실측 대비를 프론트가 결정적으로 계산 —
  실측 없는 장이 섞이면 반쪽 합계를 만들지 않는다.
- 검증: pytest 565 (gains 테스트 3 RED→GREEN), node --check. **주의**: 브리지가
  `f20_strategy.py` 를 임포트하므로 돌던 서버는 재시작+재예열해야 반영된다.
  실 LLM 으로 gains 가 실제 수치를 인용하는지 실측 1회는 아직 안 했다.

**909cae8 — 연습이 진행 중이면 새 발표를 시작할 길이 없던 것**
- 사용자: "자꾸 연습 이어하기 때문에 처음부터 발표를 할 수가 없네".
  syncTopbar 가 진행 중 세션이 있으면 탑바의 유일한 버튼을 「연습 이어하기」로
  바꿔치기해서, 새로 시작할 버튼이 화면에서 사라졌다.
- 「새 발표 연습」(주 버튼)은 항상 두고, 진행 중일 때만 옆에 「연습 이어하기」
  (보조)가 하나 더 생긴다 — 코칭 중이면 #/qa, 발표 연습 중이면 하던 단계로.
  이어하기가 주 버튼을 뺏지 않는다. 확인 다이얼로그는 안 넣었다(끝난 코칭은
  기록함에 남는다) — 필요해지면 그때 단다.
- 검증: node --check, pytest 567.

**908c708 — 탑바 여백: 56px 바가 44px 버튼을 6px 로 물고 있던 것**
- 두 버튼 구조가 되며 버튼이 바를 꽉 채워 보이는 게 두드러졌다. 64px 로 올려
  위아래 10px. 탑바 높이에 묶인 #rtabs sticky top(리포트 탭)과 tablet.css
  --topbar-h 를 같이 올렸다 (--topbar-h 훅은 병행 세션 2689d7d 에 먼저 실려 나감).

**(운영) 브리지 재시작 + 예열 — F-20 근거 층 반영**
- 병행 세션이 띄운 브리지를 내리고 재기동(경합 끝에 지금 도는 프로세스가 새
  f20 코드를 물고 있음을 확인). 예열 완료 — habits 응답 `"provider": "lora"`.
- F-20 gains 실 LLM 실측(실데이터로 「구성 제안받기」)은 여전히 미완 — 부스
  리허설 때 확인할 것.

### 같은 트리에서 동시 세션 주의
이 세션과 병행으로 다른 세션이 초록 팔레트 제거(6ac51e9)·qa_live/session_store
수정을 진행 중이었다. 커밋은 훅 단위로 선별해 서로의 미커밋 작업을 태우지
않았고, 캐시 버전은 그쪽이 올린 qa75(이후 qa77→qa79)로 수렴했다.
tablet.css 의 --topbar-h 훅 하나는 반대 방향으로 상대 커밋(2689d7d)에 쓸려
들어갔다 — 내용은 정합해서 그대로 뒀다.

## 2026-08-07 — QA 코칭 버그 수리 스프린트 (Claude 세션)

사용자 실측 제보("이전 발표가 따라온다", "정답인데 짧다고 거부된다",
"힌트에 답하면 또 unknown")에서 출발해 QA 로직 전 구간
(qa_live.js → chuckchuck_bridge.js → demo/bridge.py → f09_judge.py)을
감사하고 수리했다. 마지막에는 탐색 에이전트 3개를 병렬로 돌려 전면 감사를
한 번 더 했다. 판정 강도는 실측(정답 단답 75점 통과 / 오답 단답 39점 거부)
기준 적정으로 판단해 프롬프트 완화 이후 추가 조정하지 않았다.

### 고친 것 (커밋순)

**0169653 — 발표를 끝내고 새로 시작해도 QA 에 지난 발표의 질문·대화가 나오던 것**
- 41e01ac(이전 세션)는 「처음부터 시작」 버튼 경로만 qa 를 지웠다. 일반 `#/new`
  링크로 재진입하는 라우터 초기화와 「다른 녹음으로 다시」(againTake)가 qa 를
  안 지워, `qaLiveActive()`(질문 존재만 확인)를 통과한 지난 발표의 질문 코칭이
  그대로 그려졌다. 두 경로에 `resetQa()` 추가.
- 검증: pytest 556 유지, 수동 재현 절차 커밋 메시지에 기록.

**9287bfe — 이지선다 질문에 정답 단답이 「너무 짧아 판단할 수 없다」로 거부되던 것**
- 원인 둘 다 프롬프트: ① F-08 이 만든 정답 골자(answer_gist)가 코칭에만 실리고
  판정에는 안 실렸다. ② unknown 정의("답이 너무 짧거나")가 규칙 1("짧아도
  맞으면 good")과 프롬프트 안에서 싸웠다.
- judge_answer 가 골자를 채점 기준 블록으로 싣고, 길이 문구 제거 + 선택형
  질문 규칙 + 정답 유출 금지 규칙 추가. PRD:
  [.claude/prds/qa-judge-short-answer.prd.md](../.claude/prds/qa-judge-short-answer.prd.md)
- 검증: 실 Solar 실측 — 「얕은 수면이야」 partial 75 통과, 「깊은 수면이야」
  wrong 39 거부. pytest 559 (새 테스트 3).

**d24c1be — 되묻기·힌트 뒤의 증분 답변이 단독으로 판정되던 것**
- f09 의 누적 판정(_answer_block)이 데모 경로에서 죽은 기능이었다: 프론트가
  prior_answers 를 안 보내고 브리지가 버렸다. 되물음 문면도 판정 히스토리에
  안 남아 "네" 가 무엇에 대한 답인지 서버가 몰랐다.
- 프론트→브리지→판정까지 prior_answers 배선, 턴 기록에 실제 물었던 되물음과
  question_id 를 남김. 부수: react 칩의 "undefined" 폴백, 힌트 "N/3" 하드코딩
  을 실제 사다리 길이로.
- 검증: pytest 561 (브리지 순수 함수 RED→GREEN), 실 브리지 2턴 실측.

**bef16ff — 판정 타임아웃·실패 시 답 증발·복원 세션 흰 화면·폴링 카운터**
- 판정 전용 60초 타임아웃 분리(F-09 는 JSON 깨지면 2회 왕복), 판정 실패 시
  입력창에 답 복원, streamRow 'missing' 가드(구버전 세션 흰 화면), qaBridgeTries
  리셋, 죽은 변수 정리.
- 검증: node --check, pytest 561 유지.

**d69d701 — 전면 감사 1순위 (탐색 에이전트 3개 병렬 감사 결과)**
- **flat 세션 오염**: 세션 저장소가 None 을 건너뛰어 발표 A 의 flow 가 발표 B 의
  질문·판정 근거로 섞였다 — "이전 발표가 따라온다" 의 서버측 잔재. 명시적
  null 은 삭제로 다루도록 put_artifacts 의미 변경 (본문에 없는 키는 유지).
- **리포트 조인 키**: 코칭 기록은 'flat' 키로 저장되는데 리포트는 'imu2clip'
  (목 데모 키)을 기본으로 읽어, 실전 코칭이 안 뜨거나 목 기록이 내 것처럼
  떴다. 해시 id 가 없으면 가장 최근 기록을 읽게 변경.
- **질문 생성 경쟁**: 생성 중 이탈·복귀 시 데모 질문이 끼어들고, 늦게 도착한
  결과가 하던 대화를 지웠다. 생성 중엔 준비 화면 유지, 스킵 후 결과는 폐기.
- **판정 대기 중 새로고침**: 말풍선만 남고 상태에서 사라지던 답을 입력창
  초안으로 복원.
- **리포트 「내 답변」**: 코치 총평이 아니라 실제 답을 싣고 총평은 note 로.
- **브리지 flow 미전달**: build_questions 에 flow 가 빠져 순서 역행 개념의
  질문 이유가 엉뚱한 문구로 나왔다.
- 일부 hunk 는 병행 세션의 4b71717 에 먼저 쓸려 들어갔다 (아래 운영 메모).
- 검증: pytest 562 (put_artifacts 의미 변경 RED→GREEN).

**(후속) — 마이크 여닫는 경계의 제출 가드**
- `if (liveMic)` 가드는 여는 중(권한 팝업 대기, liveMic 아직 null)과 받아쓰는 중
  (liveMic 이미 null)을 못 막았다 — 그 틈에 Enter 로 보내면 녹음이 고아가
  되거나 받아쓴 문장이 재렌더에 지워졌다. `liveMicPending`('opening' |
  'transcribing') 상태를 추가해 양쪽 가장자리 제출을 안내 문구와 함께 막는다.
- 검증: node --check, pytest 초록 유지.

**(후속 2) — 감사 잔여 7건 일괄 수리 (중 2 · 하 5)**
- qa-history: 파싱 실패 시 손상 원본을 `:corrupt` 키로 백업해 30건 일괄 소실을
  막고, 종료 화면의 "코칭 기록 저장됨" 라벨이 실제 저장 성공 여부를 말한다.
- 체크포인트 카드: explanation(막힘 코칭 해설)을 gist 보다 앞세워 죽은 분기
  해소 — explain 코칭에서만 채워지므로 일반 경로 표시는 불변.
- rate limit: LLM 을 부르는 판정(플랫·세션 경로)·전략을 PAID_PATHS 에 추가 —
  rubric 만 막던 비대칭 해소. 분당 30이라 정상 사용엔 안 걸린다.
- with_hint_ladders 를 f08 로 단일 출처화, FastAPI 동기 라우트·잡 큐에도 적용
  (브리지는 재수출) — `?api=` 직결 시 사다리 1칸 무너짐 해소.
- 폴백 질문 힌트에 슬라이드 3장 절단(2단과 다른 문구로 중복 제거 회피),
  trap 질문 gist 폴백에 전제 반박 문구, 목업 기록 turns 배열 → 숫자.
- 검증: pytest 567 (새 테스트 3 — 절단·trap 골자·서버 힌트 계약), node --check.

**5b562f3 — 질문 코칭 화면이 스텝4로 덮여 「질문 코칭 시작하기」가 안 먹던 것**
- refreshStep4IfVisible 의 라우트 가드 정규식 `/^#\/?(new)?(\/|$)/` 이 모든
  해시에 참이었다 (node 실측 5/5 오탐). 파이프라인 후반부 완료 틱이 #/qa 화면을
  스텝4로 덮었고, 해시가 이미 #/qa 라 시작 버튼(href=#/qa)이 무반응이 됐다.
  라우트 키를 잘라 정확히 'new' 일 때만 다시 그린다. 갇힌 화면은 새로고침이면
  풀린다.
- 검증: node 실측(수정 후 new 계열만 참), node --check, pytest 567.

**(UX) — 판정 후 전체 화면 체크포인트 제거, 대화 유지**
- 사용자: "정답을 맞췄을 때 팝업으로 넘어가는 ux 는 별로, 대화 형식이 유지되면
  좋겠어". 통과·해설·답 공개 세 경로가 전체 화면 학습 카드(checkpoint)로
  갈아타던 것을, 말풍선(방어 표식·총평·모범답)을 남기고 같은 스트림에서 다음
  질문으로 이어지게 바꿨다. 이전에 저장된 세션의 checkpoint 는 열리는 순간
  기록만 닫고 이어간다. renderLiveCheckpoint 는 죽은 코드로 남김(마감 전
  리팩터 금지).
- 검증: node --check, pytest 567 유지.

**(UX) — 연습 도구 4종을 실데이터 기반으로**
- 사용자: "제안 받기가 아니라 자동으로 나오면 좋겠어. 개요 이미지·펀치라인·
  용어카드도 전부 발표와 슬라이드 기반으로". ① 발표 구성(F-20)은 실데이터
  세션이면 버튼 없이 자동 요청(페이지 수명당 1회 가드 — 실패 반복이 과금
  루프가 되지 않게, 샘플은 버튼 유지). ② 개요 이미지는 개념 그래프·판정으로
  노드를 만들고 좌표를 노드 수에서 계산(고정 좌표표는 샘플 9칸 전용이었다).
  ③ 펀치라인은 예상 질문의 모범답(answer_gist) 우선, 없으면 그래프 핵심
  문장. ④ 용어 카드는 그래프 요약(정의)+판정 상태+F-08 질문·골자. 재료가
  없으면 샘플로 위장하지 않고 빈 상태를 말한다.
- 검증: node --check, pytest 567 유지. 실데이터 육안 확인은 리허설에서.

### 남은 것

없음 — 감사에서 확인된 항목은 전부 수정됐다. 이후 버그는 새로 발견되는 대로
이 일지에 적는다.

### 운영 메모

- 브리지는 `DEMO_PORT=8799 ./demo/run_bridge_midm.sh` 로 실행 중, LoRA 예열
  완료(provider=lora). 서버 코드(bridge.py·session_store.py)를 고치면 반드시
  재시작 + 재예열해야 한다.
- **병행 세션 주의**: 같은 날 다른 Claude 세션이 UI 개편을 병행하며 캐시
  버전(?v=)을 qa63→qa54→qa55→qa56→qa73/74 로 자기 계보로 되돌리는 일이
  반복됐다. 과거에 서빙된 값으로 내려가면 옛 캐시를 든 브라우저가 낡은
  파일을 쓴다 — 버전은 **어느 계보에서든 한 번도 안 쓴 값으로만 올린다**
  (현재 qa75). 두 세션이 같은 파일을 만지면 미커밋 수정이 상대 커밋에 쓸려
  들어간다 — 한쪽을 멈추거나 담당 화면을 나누는 것을 권한다.
- 지난 QA 감사(11건)의 완결 기록은 [QA_FIXES.md](QA_FIXES.md) 에 있다.
