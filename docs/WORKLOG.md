<!-- 이 파일: 날짜별 작업 일지입니다. 세션(사람·AI)마다 무슨 작업을 왜 했는지 남깁니다.

작성 규칙
- 최신 날짜가 위로 온다. 같은 날 여러 세션이면 `## 날짜 — 제목 (작업자)` 절을 나눠 쓴다.
- 한 덩어리(커밋 단위)마다: 커밋 해시 · 무엇을 · 왜 · 어떻게 검증했는지.
- "왜"가 핵심이다 — 무엇을 고쳤는지는 diff 가 말한다 (CLAUDE.md §3-5 와 같은 규율).
- 완결된 대형 감사 기록은 별도 문서로 두고 여기서 링크한다 (예: [QA_FIXES.md](QA_FIXES.md)).
-->

# 작업 일지

## 2026-08-09 — Q&A 코칭 최종 점검 (2026-08-08 시작, Claude 세션)

"Q&A 최종 점검을 하자"에서 출발했다. 지목된 것은 다섯 — 새 발표 시작 시 세션
초기화, 이전 QA 값이 남는 게 맞는지, 정답 판정이 너무 광범위한 것, 힌트, 마이크·
입력. 진행 중에 "자료와 다른 녹음을 올리면 아예 인식을 못 한다" 가 추가됐다.

**이 세션의 성격: 고친 것보다 「고치려다 측정에 막힌 것」이 더 많다.** 가설 셋이
숫자 앞에서 뒤집혔고, 그때마다 프롬프트·임계값을 만지는 대신 계획서를 고쳤다.

### 산출물 (커밋순)

**2b2e516 — 측정에 쓸 slidedoc fixture 고정**
- Upstage 재파싱 지터로 디스크와 git 이 어긋나 있었다. 판정을 재기 전에 바닥을
  고정하지 않으면 나중에 숫자를 되짚을 근거가 사라진다. 33장·8장 구조는 그대로.

**2261a14 — 판정이 무른지 재는 도구를 만들고, 원인이 F-09 가 아님을 확인**
- `examples/qa_judge_probe.py`. 실 LLM(solar)으로 질문 3개 × 답 5벌 = 18 판정.
- **결과가 가설을 뒤집었다.** 무관한 답 → wrong 31.7점, 얕은 답 → partial 75 로
  안 닫힘, 기대 골자 → good 81.7. `qa_mastered` 게이트가 설계대로 돈다.
  → **`QA_PASS_SCORE` 도 f09 규칙 8 대역도 건드리지 않았다.**
- 진짜 원인은 F-08 질문 중복이고, **5분 트랙에서만** 생긴다 (후보 13개 중 서로
  답이 겹치는 상위 3개만 뽑히기 때문). 10분 기본 트랙은 0.40 넘게 겹치는 쌍이
  21개 중 1개. → 코드 변경 없이 **시연을 10분 트랙으로** 하는 것이 답이다.

**ab4133a — 녹음이 자료와 다르면 구간 추정을 버리고, 왜 못 맞췄는지 화면에 말한다**
- 저장된 fixture 에서 재현했다: `MIN_CONFIDENCE 0.12` 를 0.19 로 겨우 넘겨
  "추정 성공" 이 되고, 2·3·4장이 폭 0 구간으로 남아 발화가 통째로 비었다.
  그게 "인식을 못 한다" 의 실체였다.
- `MAX_EMPTY_RATIO = 0.25` 가드 추가. **`MIN_CONFIDENCE` 는 일부러 안 올렸다** —
  올리면 멀쩡한 추정까지 떨어진다. `marks_reason` 을 화면에 정직하게 싣는다.

**5c1ca6d — 확실한 버그 셋**
- **마이크**: `hasLiveDictation()` 이 생성자 존재만 봐서, 받아쓰기가 망 오류로
  한 번 죽으면 **다시 눌러도 같은 죽은 경로로 들어갔다.** `liveDictationDead`
  세션 플래그를 세우고 그 뒤로는 녹음 폴백으로 내려간다.
- **새 발표 = 새 QA**: `resetQa()` 호출 지점 6곳 중 **업로드 경로에 없었다.**
  `docKey`(파일명+장수) 지문을 `qaLiveActive()` 가 보게 하고, `applySlideDoc()`
  에서도 지운다. 옛 세션의 `undefined` 는 낡음으로 안 쳐서 새로고침에 안 날린다.
- **말풍선 중복**: `revealHalf` 와 `askAgain` 이 같은 `missing_points` 를 연달아
  밀어 넣었다. `revealHalf` 가 펼쳤는지 반환하게 하고 `askAgain` 이 건너뛴다.

**312fe06 — 발표 중 「처음부터」가 녹음기를 놓아주지 않았다**
- `ccRuntime` 을 안 끝내서 화면만 처음으로 가고 녹음은 계속 돌았다.
  `stopLiveRehearsal()` 을 만들어 리셋 세 경로에서 부른다.

**3fd745a · 988247e — 우리가 추론한 개념 배치를 발표자가 고른 것처럼 묻던 것**
- 제보: 수면 발표인데 "왜 다른 **정렬 방식** 대신 이 방식을 선택했나요" 가 나왔고,
  모범답이 "**경로(위계)에서** 수면의 질 > 회복으로 표시되어 있습니다" 였다.
  발표자는 그 배치를 **본 적이 없다** — 우리 프롬프트 발판을 자료인 척 인용한 것.
- 프롬프트로 한 번(3fd745a), 그다음 **결정적 가드로** 한 번 더(988247e).
  `_cites_scaffold()` 는 **낱말이 아니라 인용 꼴**(`경로(위계)`·`연결=`·`위계로 표시`)
  로 잡아서, 자료가 진짜 쓰는 "정렬 방식"(IMU2CLIP)·"최단 경로"는 살아남는다.
  테스트로 고정했다.
- **여기서도 설계 하나가 측정에 기각됐다.** 모범답을 자료와 대조해 겹침이 낮으면
  버리려 했으나 신호가 거꾸로였다: 좋은 답 9%·10% 대 살붙인 답 24%, 발판 유출은
  0%. F-08 이 `SlideDoc` 을 안 받고 개념 요약(토큰 9개)만 받기 때문이다.
  → 바닥값을 넣지 않았다. **「뇌파 측정을 통해」류 살붙임은 아직 못 막는다.**

**ad6de9d — 자료와 무관한 녹음이면 「아예 다른 내용」이라고 판정해서 말한다**
- "잘 맞지 않아요" 는 구간을 못 맞췄다는 말이지 **왜** 못 맞췄는지가 아니다.
  다른 파일을 올린 사람은 그 문구로 원인을 못 찾는다.
- 정렬과 무관한 전역 신호 둘(IDF 가중 어휘 겹침 · 발화 창의 슬라이드 커버율)이
  **둘 다** 임계 아래일 때만 단정한다. 임계 0.2/0.6 은 실녹음 2종을 자료 6종에
  교차로 붙인 실측으로 정했다. 한국어끼리 흔한 낱말이 겹치는 경계 사례(RINGLE
  0.258)는 **단정하지 않는다** — 확신 없는 단정이 더 나쁘다.
- `marks_match` 로 리포트 맨 위 경고까지 올린다. 저장 리포트 복원 경로에도 남긴다.
- 검증: 수면 녹음을 자료 9종에 붙여 맞는 짝 2종은 통과(0.671), 불일치 6종은 전부
  `unrelated`. focus 자료는 conf 0.227 로 **옛 가드를 통과하던 값**이라 특히 중요.

**a13f989 · 65334df — 마감 정산과 근거 정정**
- 계획서에 "일부러 안 한 것" 을 이유와 함께 남겼다. CLAUDE.md 회귀 기준선이
  552(§3-5)·498(§4)로 서로 다르고 둘 다 낡아서 실측 **591** 로 맞췄다.
- **65334df 는 내가 어제 적은 근거를 뒤집은 커밋이다.** W4-b(출구 조건)를 유지하는
  이유로 "코드 주석이 의도를 기록하고 있다" 를 댔는데 틀렸다 — 그 주석의 의도는
  조건을 AND→OR 로 바꾼 것이지 `<=` 가 아니고, `<` 로 바꿔도 힌트 소진 분기는
  살아 있어 옛 버그는 안 돌아온다. 결론이 맞아도 근거가 틀리면 결론을 못 지킨다.

### 일부러 안 한 것

- **W4-b 출구 조건**: 유지. 진짜 근거는 **출구를 눌러도 리포트가 안 부푼다**는 것.
  `closeRetell` 이 답을 본 뒤의 발화를 판정하지 않고, `revealed` 는 `bucketOf`·
  연속 기록·방어 집계·리포트 `skipped` 네 곳에서 일관되게 빠진다. **답을 보고
  통과하는 길이 구조적으로 없다** → "판정이 광범위한 체감의 절반이 여기일 수
  있다"는 W3 가설도 같이 기각. 그리고 `<=` 가 `liveStalled` 라는 이름에 맞는다 —
  점수가 제자리인 것이 곧 막힌 것이고, 좁히면 정작 막힌 사람이 출구를 잃는다.
- **W4-a 힌트 장 번호 구조 전송**: 전제는 맞지만 프론트 정규식이 서버가 실제로
  쓰는 문장 형식 전부에서 맞는 답을 낸다. 마감일에 양쪽을 건드릴 값어치가 없다.
- **W4-c 힌트 분모 흔들림**: 실재하나 미용. 말풍선에 `total` 을 싣게 고친 뒤로
  거짓 숫자("4/3")는 안 뜬다. 고칠 자리는 계획서에 적었다.
- **W5-b 입력 카드 버튼 6개**: MVP_SPEC §3 위반 맞으나 마감일 레이아웃 수정 보류.
- **W2(c) 서버 세션 키 분리**: `liveArtifacts()` 가 다섯 키를 늘 보내므로 오늘
  새는 데가 없다. 마감 뒤 보험.

### 검증

- `python -m pytest tests/ -q` → **591 passed · 7 skipped** (세션 시작 581).
  새 테스트 10개: 구간 추정 가드 3 · 발판 인용 차단 3 · `match` 4단 상태 4.
- 브라우저 확인은 사용자가 수행("거의 다 고쳐졌어"). **`tests/` 23개가 전부
  파이썬이라 프론트 JS 는 자동 커버리지가 0이다** — 세션 초기화·마이크 폴백·
  「처음부터」·말풍선 중복은 원리상 눌러야만 확인된다.
- 코드로 확인한 것: Enter 입력이 `shiftKey`(줄바꿈)와 **`isComposing`/`keyCode 229`
  (한글 조합 중)** 를 거른다. 힌트 맥락이 `hintsShown` → 어댑터 스네이크 변환 →
  `bridge.py` → `f09` 4단으로 무결하게 전달된다 (카멜↔스네이크 유실 없음).

### 운영에서 걸린 것 (다음에 시간 날리지 않게)

- **브리지는 파이썬을 메모리에 올린 채 돈다.** 17:15 에 뜬 프로세스로 18:44~20:40
  커밋 셋을 검증하려 했는데 **하나도 반영돼 있지 않았다.** JS 는 매 요청 디스크에서
  읽어 즉시 반영되지만 파이썬은 아니다 → **파이썬을 고쳤으면 재기동 후 예열한다.**
- `pkill -f "python -m demo.bridge"` 는 **자기 셸까지 죽인다** (명령줄이 패턴과
  일치). keepalive 도 같이 날아가서 손으로 다시 세웠다.
- keepalive 가 두 개 돌고 있었다. 둘이 동시에 재기동하면 서로 죽인다 — 하나만 남긴다.

### 이어서 — 「일부러 안 한 것」 되짚기 (2026-08-10 새벽)

위 정산에서 미룬 것들을 백로그([qa-followups.plan.md](../.claude/plans/qa-followups.plan.md))
로 남기고 순서대로 열었다. **이번에도 측정이 계획서를 고쳤다.**

**dac6239 — 입력 카드의 버튼 여섯을 두 줄로 나눈다** (위 W5-b 해제)
- 주 행동 둘(보내기·마이크)만 한 줄에 두고 나머지를 보조 줄로 내렸다. 버튼은
  하나도 안 없앴다 — `liveReveal` 은 판정 실패 시 유일한 탈출구이고 「여기까지」는
  다크패턴 방지상 항상 있어야 한다.
- **두 줄 다 `step-actions` 클래스를 유지해야 한다.** 판정 중 잠금이
  `$('.qa-live-input .step-actions button')` 로 걸려 있어서, 보조 줄에서 이 클래스를
  빼면 판정 중에도 눌린다.

**efefcfe — 골자가 사실상 같은 질문을 걸러낸다** (위 W3 의 진짜 원인)
- 5분 트랙에서 세 질문의 모범답이 사실상 같아 "아무 답이나 맞는 것처럼" 보이던 것.
  `answer_gist` 토큰 겹침으로 결정적으로(LLM 없이) 걸러낸다.
- **백로그에 적어 둔 임계값 0.40 을 그대로 못 썼다.** 그 숫자는 측정 도구의
  토크나이저에서 나온 것이라 여기 토크나이저로는 **진짜 쌍둥이 셋 중 둘(0.24·0.26)을
  놓쳤다.** 직접 재서 0.20 으로 잡았다 (쌍둥이 0.24~0.43 대 다른 골자 0.00~0.04).
- **질문 개수를 절대 안 줄인다.** 여유분을 짧은 트랙에만 줘서 10분 기본 트랙은
  되채우기로 결과가 종전과 동일하다 — 시연 경로는 구조적으로 무영향.

**a93a974 — 브라우저 없이 도는 프론트 스모크** (위 「검증」의 *JS 커버리지 0* 해제)
- `qa_live.js` 는 최상위에 부작용이 없어 `vm` 컨텍스트에 통째로 올린다. `app.js` 는
  최상위에서 `route()` 가 돌아 못 올리므로 `qaDocKey` 하나만 이름으로 잘라 쓴다.
  못 찾으면 폴백 없이 던진다 — 손으로 베낀 사본을 두면 그때부터 원본이 아니라
  사본을 시험하게 된다.
- **전역 스텁이 곧 시험 범위다.** `hasRealSlideImage`·`qaDocKey` 는 원본이
  `typeof … === 'function'` 으로 감싸고 있어 안 심으면 그 가지가 죽은 코드가 된다.
- **마지막 케이스가 하네스 자신을 검사한다** — 고치기 전 `liveHints()` 를 소스
  문자열 위에서 되돌려 같은 단언이 반드시 깨지는 것을 확인한다.

**eb968a8 — 세션 아티팩트 키 계약** (위 W2(c) 해제)
- 세션 키를 나누려다 전제를 먼저 쟀더니, "새는 데 없다" 가 참일 뿐 아니라 **설계로**
  지켜지고 있었다. `put_artifacts` 는 명시적 `None` 을 지우기로 계약했고 docstring 이
  사건까지 기록해 뒀다.
- 그래서 키를 나누는 대신 **계약이 깨지는 유일한 조건**을 잠갔다 — `ARTIFACT_KEYS` 에
  여섯 번째 키가 늘고 `liveArtifacts()` 가 안 따라오는 경우.
- 키를 안 나눈 이유: 지금 새는 데가 없는데 바꾸면 브리지의 기존 아티팩트 조회가
  안 닿는다. 마감일에 관측되는 이득 없이 위험만 든다.

**아직 안 한 것 — 백로그 4번(SlideDoc → F-08).** 값어치는 제일 크지만 F-08 프롬프트에
슬라이드 본문을 넣는 변경이라 모든 발표에서 토큰·지연·과금이 늘고, 검증에 실 LLM 을
여러 번 돌려야 한다. 실패하면 질문 생성 자체가 망가진다 — 마감 뒤 과제.

**검증**: `node tests/js/qa_live.smoke.mjs` 25 케이스 · `python -m pytest tests/ -q`
**597 passed · 7 skipped**. 브라우저 클릭은 입력 카드 두 줄(`?v=qh3`)이 남아 있다.

## 2026-08-09 — 리포트 정보 다이어트 + 탭 전환 최적화 + QA→리포트 E2E 점검 (Claude 세션)

"마지막 리포트가 정보의 바다"라는 관찰에서 출발. 요약 탭을 열면 여덟 덩어리
(판정 헤드·복습 카드·트로피·덱·개념 판정·코칭 내역·보완 3·CTA)가 동시에 말을
걸어 결론이 안 읽혔다. 방향 합의: **덱은 데모의 주인공이라 펼쳐 두고, 나머지를
예고형 접기로** — 전역 「핵심만 보기」 모드는 마감일 회귀 위험이 커서 버렸다.

**5270c02 — 요약 탭 2층 구조 · switchReportTab · E2E 버그 2건**

- 정보 다이어트: 기본 노출은 결론 층(판정 헤드·오늘 가져갈 문장·슬라이드 덱·
  보완 1가지)만. 코칭 내역·복습 한 마디는 접기로 내리되, 접힌 줄이 열 가치를
  먼저 말한다 — 「교수님과 주고받은 3개 질문 · 대화로 2개 늘었어요」,
  「11개 · 다시 볼 곳 3개」 + 다시 볼 곳이 있을 때만 빨간 점(유도).
  기존 judge-fold 관례 재사용, 새 문법 발명 없음.
- 탭 전환 최적화: 탭마다 renderReport() 전체를 다시 불러 왼쪽 판정 헤드
  (완성도 점수·차원 막대)가 재렌더·재애니메이션됐다. 본문(#rbody)만 갈아끼우는
  switchReportTab 으로 분리 — async 렌더 직후 DOM 을 짚던 goRubric·goJudge 의
  경주도 같이 사라졌다(이제 동기).
- E2E 점검(QA 코칭 기록 → 리포트 → 연습 도구) 중 잡은 둘:
  ① recordQaHistory 실전 경로가 「질문 전/후」를 샘플(3/5)에서 베껴 적었다 —
  질문 4개짜리 코칭에 「4개 중 5개 늘었어요」가 가능했다. 실기록에서 센다:
  질문 후 = 최종 설명해낸 것(full), 질문 전 = 그중 힌트·재시도 없이 첫 답에
  설명한 것. ② #/report/last·sample-imu2clip 주소를 그대로 QaHistory 키로
  조회해 코칭 내역이 소리 없이 사라졌다 — last 는 최근 기록, 샘플은 목
  키(imu2clip)로 해석. 연습 도구는 이미 실데이터 도출 + 정직한 빈 상태가
  있어 손댈 것 없음.
- 검증: pytest 591 초록 · node --check · Playwright 헤드리스로 DOM 검사
  11항목(헤드 노드 유지 왕복·접기 기본 닫힘·메타 문구·last 해석) + 콘솔 에러
  없음 + 스크린샷 육안. app.js ?v=qg6.
- **남은 것**: 스펙 §5.4의 「발표에서는 언급만 함 → 대화로 이해함」 변화가
  개념 판정 트리에 아직 반영 안 됨 — 판정은 파이프라인 결과만 보고 QA 결과는
  코칭 내역 접기에만 있다. 마감일이라 안 건드림. 실데이터 리포트(브리지) 육안
  확인도 리허설 때 할 것.

## 2026-08-07 — 상세리포트 「논리 흐름·음성 습관」 탭 재편 (Claude 세션)

"두 탭이 별로 와닿지 않는다"는 관찰(본인+팀원)에서 출발했다. 판정 카드·막대·
숫자는 나열돼 있는데 부스 심사위원이 30초 훑을 때 "가장 큰 문제가 뭐고 뭘
하라는 건지"가 안 읽혔다. PRD → 계획 → 구현 순서로 진행.

### 산출물 (커밋순)

**1ec83a5 — PRD: 두 탭을 누구 기준으로 왜 고치는지 확정**
- 1차 독자를 부스 심사위원·관람객으로, 성공 기준을 「처음 보는 사람이 3초 안에
  문제+처방을 자기 말로 재현」으로 못박았다. 파이프라인 변경은 범위 밖.
- [.claude/prds/report-tabs-resonance.prd.md](../.claude/prds/report-tabs-resonance.prd.md)

**4b71717 — 두 탭에 진단→근거→처방 구조 (마일스톤 1~3 구현)**
- 탭 첫 화면에 「가장 큰 문제 한 줄 + 처방 한 줄」 진단 블록(`.tab-verdict`).
  우선순위 규칙: 논리 흐름은 연결 멘트 없음 > 근거 점프, 음성 습관은 핵심 장
  시간 부족·초과 > 속도 > 간투어. 1순위 근거 카드만 펼치고 나머지는 fold 로
  접는다 (rSummary 의 기존 관례 재사용 — 새 문법 발명 없음).
- 남는 숫자 전부에 판정 말을 붙였다: 일치도 % → 「자료 순서를 잘 따랐어요」,
  자/분 → 「권장보다 조금 빨라요」, 쓴 시간 → 「목표보다 N분 길었어요」.
  숫자와 판정 색 5종은 불변 (§14 숫자는 신성하다).
- 정직성: 근거가 없으면 헤드라인을 지어내지 않고 블록째 생략. 실데이터·샘플
  경로 모두 같은 구조, 샘플 배지 유지. 기존 voice-tip 처방 문구는 진단 블록으로
  통합해 중복 제거. 파이프라인·스키마는 손대지 않았다.
- 계획: [.claude/plans/report-tabs-resonance.plan.md](../.claude/plans/report-tabs-resonance.plan.md)
- 검증: node 스모크 11케이스(우선순위·빈 데이터·이슈 없음·조사 을/를),
  `node --check`, pytest 562 초록. **남은 것**: 아이패드 가로 첫 화면 실측과
  브리지 실데이터 육안 확인 — 리포트를 열면 객석 수다 프리페치가 실 API 를
  물어 과금되므로 자동으로 열지 않았다. 리허설 때 확인 후 마일스톤 완료 처리.

**bbbf6c9 — 구성 제안(F-20)에 「왜 바꿔야 하는가」 근거 층**
- "발표 형식 의논(잡스식 등)의 피드백이 안 와닿는다"는 제보. 통보 형식(1안
  확정)은 유지하기로 하고, 순서표(뭘 하라)에 빠져 있던 효과(왜 좋은가)를 채웠다.
- ① LLM 이 gains(claim+evidence) 2~3개를 내되 evidence 가 입력의 실제 개념·구간
  이름·시각을 못 짚으면 서버가 버린다 — keep.quote 와 같은 규율. ② 순서표
  구간마다 「지금 4:10 → 제안 1:30」 실측 대비를 프론트가 결정적으로 계산 —
  실측 없는 장이 섞이면 반쪽 합계를 만들지 않는다.
- 검증: pytest 565 (gains 테스트 3 RED→GREEN), node --check. **주의**: 브리지가
  `f20_strategy.py` 를 임포트하므로 돌던 서버는 재시작+재예열해야 반영된다.
  실 LLM 으로 gains 가 실제 수치를 인용하는지 실측 1회는 아직 안 했다.

**909cae8 — 연습이 진행 중이면 새 발표를 시작할 길이 없던 것**
- 사용자: "자꾸 연습 이어하기 때문에 처음부터 발표를 할 수가 없네".
  syncTopbar 가 진행 중 세션이 있으면 탑바의 유일한 버튼을 「연습 이어하기」로
  바꿔치기해서, 새로 시작할 버튼이 화면에서 사라졌다.
- 「새 발표 연습」(주 버튼)은 항상 두고, 진행 중일 때만 옆에 「연습 이어하기」
  (보조)가 하나 더 생긴다 — 코칭 중이면 #/qa, 발표 연습 중이면 하던 단계로.
  이어하기가 주 버튼을 뺏지 않는다. 확인 다이얼로그는 안 넣었다(끝난 코칭은
  기록함에 남는다) — 필요해지면 그때 단다.
- 검증: node --check, pytest 567.

**908c708 — 탑바 여백: 56px 바가 44px 버튼을 6px 로 물고 있던 것**
- 두 버튼 구조가 되며 버튼이 바를 꽉 채워 보이는 게 두드러졌다. 64px 로 올려
  위아래 10px. 탑바 높이에 묶인 #rtabs sticky top(리포트 탭)과 tablet.css
  --topbar-h 를 같이 올렸다 (--topbar-h 훅은 병행 세션 2689d7d 에 먼저 실려 나감).

**(운영) 브리지 재시작 + 예열 — F-20 근거 층 반영**
- 병행 세션이 띄운 브리지를 내리고 재기동(경합 끝에 지금 도는 프로세스가 새
  f20 코드를 물고 있음을 확인). 예열 완료 — habits 응답 `"provider": "lora"`.
- F-20 gains 실 LLM 실측(실데이터로 「구성 제안받기」)은 여전히 미완 — 부스
  리허설 때 확인할 것.

### 같은 트리에서 동시 세션 주의
이 세션과 병행으로 다른 세션이 초록 팔레트 제거(6ac51e9)·qa_live/session_store
수정을 진행 중이었다. 커밋은 훅 단위로 선별해 서로의 미커밋 작업을 태우지
않았고, 캐시 버전은 그쪽이 올린 qa75(이후 qa77→qa79)로 수렴했다.
tablet.css 의 --topbar-h 훅 하나는 반대 방향으로 상대 커밋(2689d7d)에 쓸려
들어갔다 — 내용은 정합해서 그대로 뒀다.

## 2026-08-07 — QA 코칭 버그 수리 스프린트 (Claude 세션)

사용자 실측 제보("이전 발표가 따라온다", "정답인데 짧다고 거부된다",
"힌트에 답하면 또 unknown")에서 출발해 QA 로직 전 구간
(qa_live.js → chuckchuck_bridge.js → demo/bridge.py → f09_judge.py)을
감사하고 수리했다. 마지막에는 탐색 에이전트 3개를 병렬로 돌려 전면 감사를
한 번 더 했다. 판정 강도는 실측(정답 단답 75점 통과 / 오답 단답 39점 거부)
기준 적정으로 판단해 프롬프트 완화 이후 추가 조정하지 않았다.

### 고친 것 (커밋순)

**0169653 — 발표를 끝내고 새로 시작해도 QA 에 지난 발표의 질문·대화가 나오던 것**
- 41e01ac(이전 세션)는 「처음부터 시작」 버튼 경로만 qa 를 지웠다. 일반 `#/new`
  링크로 재진입하는 라우터 초기화와 「다른 녹음으로 다시」(againTake)가 qa 를
  안 지워, `qaLiveActive()`(질문 존재만 확인)를 통과한 지난 발표의 질문 코칭이
  그대로 그려졌다. 두 경로에 `resetQa()` 추가.
- 검증: pytest 556 유지, 수동 재현 절차 커밋 메시지에 기록.

**9287bfe — 이지선다 질문에 정답 단답이 「너무 짧아 판단할 수 없다」로 거부되던 것**
- 원인 둘 다 프롬프트: ① F-08 이 만든 정답 골자(answer_gist)가 코칭에만 실리고
  판정에는 안 실렸다. ② unknown 정의("답이 너무 짧거나")가 규칙 1("짧아도
  맞으면 good")과 프롬프트 안에서 싸웠다.
- judge_answer 가 골자를 채점 기준 블록으로 싣고, 길이 문구 제거 + 선택형
  질문 규칙 + 정답 유출 금지 규칙 추가. PRD:
  [.claude/prds/qa-judge-short-answer.prd.md](../.claude/prds/qa-judge-short-answer.prd.md)
- 검증: 실 Solar 실측 — 「얕은 수면이야」 partial 75 통과, 「깊은 수면이야」
  wrong 39 거부. pytest 559 (새 테스트 3).

**d24c1be — 되묻기·힌트 뒤의 증분 답변이 단독으로 판정되던 것**
- f09 의 누적 판정(_answer_block)이 데모 경로에서 죽은 기능이었다: 프론트가
  prior_answers 를 안 보내고 브리지가 버렸다. 되물음 문면도 판정 히스토리에
  안 남아 "네" 가 무엇에 대한 답인지 서버가 몰랐다.
- 프론트→브리지→판정까지 prior_answers 배선, 턴 기록에 실제 물었던 되물음과
  question_id 를 남김. 부수: react 칩의 "undefined" 폴백, 힌트 "N/3" 하드코딩
  을 실제 사다리 길이로.
- 검증: pytest 561 (브리지 순수 함수 RED→GREEN), 실 브리지 2턴 실측.

**bef16ff — 판정 타임아웃·실패 시 답 증발·복원 세션 흰 화면·폴링 카운터**
- 판정 전용 60초 타임아웃 분리(F-09 는 JSON 깨지면 2회 왕복), 판정 실패 시
  입력창에 답 복원, streamRow 'missing' 가드(구버전 세션 흰 화면), qaBridgeTries
  리셋, 죽은 변수 정리.
- 검증: node --check, pytest 561 유지.

**d69d701 — 전면 감사 1순위 (탐색 에이전트 3개 병렬 감사 결과)**
- **flat 세션 오염**: 세션 저장소가 None 을 건너뛰어 발표 A 의 flow 가 발표 B 의
  질문·판정 근거로 섞였다 — "이전 발표가 따라온다" 의 서버측 잔재. 명시적
  null 은 삭제로 다루도록 put_artifacts 의미 변경 (본문에 없는 키는 유지).
- **리포트 조인 키**: 코칭 기록은 'flat' 키로 저장되는데 리포트는 'imu2clip'
  (목 데모 키)을 기본으로 읽어, 실전 코칭이 안 뜨거나 목 기록이 내 것처럼
  떴다. 해시 id 가 없으면 가장 최근 기록을 읽게 변경.
- **질문 생성 경쟁**: 생성 중 이탈·복귀 시 데모 질문이 끼어들고, 늦게 도착한
  결과가 하던 대화를 지웠다. 생성 중엔 준비 화면 유지, 스킵 후 결과는 폐기.
- **판정 대기 중 새로고침**: 말풍선만 남고 상태에서 사라지던 답을 입력창
  초안으로 복원.
- **리포트 「내 답변」**: 코치 총평이 아니라 실제 답을 싣고 총평은 note 로.
- **브리지 flow 미전달**: build_questions 에 flow 가 빠져 순서 역행 개념의
  질문 이유가 엉뚱한 문구로 나왔다.
- 일부 hunk 는 병행 세션의 4b71717 에 먼저 쓸려 들어갔다 (아래 운영 메모).
- 검증: pytest 562 (put_artifacts 의미 변경 RED→GREEN).

**(후속) — 마이크 여닫는 경계의 제출 가드**
- `if (liveMic)` 가드는 여는 중(권한 팝업 대기, liveMic 아직 null)과 받아쓰는 중
  (liveMic 이미 null)을 못 막았다 — 그 틈에 Enter 로 보내면 녹음이 고아가
  되거나 받아쓴 문장이 재렌더에 지워졌다. `liveMicPending`('opening' |
  'transcribing') 상태를 추가해 양쪽 가장자리 제출을 안내 문구와 함께 막는다.
- 검증: node --check, pytest 초록 유지.

**(후속 2) — 감사 잔여 7건 일괄 수리 (중 2 · 하 5)**
- qa-history: 파싱 실패 시 손상 원본을 `:corrupt` 키로 백업해 30건 일괄 소실을
  막고, 종료 화면의 "코칭 기록 저장됨" 라벨이 실제 저장 성공 여부를 말한다.
- 체크포인트 카드: explanation(막힘 코칭 해설)을 gist 보다 앞세워 죽은 분기
  해소 — explain 코칭에서만 채워지므로 일반 경로 표시는 불변.
- rate limit: LLM 을 부르는 판정(플랫·세션 경로)·전략을 PAID_PATHS 에 추가 —
  rubric 만 막던 비대칭 해소. 분당 30이라 정상 사용엔 안 걸린다.
- with_hint_ladders 를 f08 로 단일 출처화, FastAPI 동기 라우트·잡 큐에도 적용
  (브리지는 재수출) — `?api=` 직결 시 사다리 1칸 무너짐 해소.
- 폴백 질문 힌트에 슬라이드 3장 절단(2단과 다른 문구로 중복 제거 회피),
  trap 질문 gist 폴백에 전제 반박 문구, 목업 기록 turns 배열 → 숫자.
- 검증: pytest 567 (새 테스트 3 — 절단·trap 골자·서버 힌트 계약), node --check.

**5b562f3 — 질문 코칭 화면이 스텝4로 덮여 「질문 코칭 시작하기」가 안 먹던 것**
- refreshStep4IfVisible 의 라우트 가드 정규식 `/^#\/?(new)?(\/|$)/` 이 모든
  해시에 참이었다 (node 실측 5/5 오탐). 파이프라인 후반부 완료 틱이 #/qa 화면을
  스텝4로 덮었고, 해시가 이미 #/qa 라 시작 버튼(href=#/qa)이 무반응이 됐다.
  라우트 키를 잘라 정확히 'new' 일 때만 다시 그린다. 갇힌 화면은 새로고침이면
  풀린다.
- 검증: node 실측(수정 후 new 계열만 참), node --check, pytest 567.

**(UX) — 판정 후 전체 화면 체크포인트 제거, 대화 유지**
- 사용자: "정답을 맞췄을 때 팝업으로 넘어가는 ux 는 별로, 대화 형식이 유지되면
  좋겠어". 통과·해설·답 공개 세 경로가 전체 화면 학습 카드(checkpoint)로
  갈아타던 것을, 말풍선(방어 표식·총평·모범답)을 남기고 같은 스트림에서 다음
  질문으로 이어지게 바꿨다. 이전에 저장된 세션의 checkpoint 는 열리는 순간
  기록만 닫고 이어간다. renderLiveCheckpoint 는 죽은 코드로 남김(마감 전
  리팩터 금지).
- 검증: node --check, pytest 567 유지.

**(UX) — 연습 도구 4종을 실데이터 기반으로**
- 사용자: "제안 받기가 아니라 자동으로 나오면 좋겠어. 개요 이미지·펀치라인·
  용어카드도 전부 발표와 슬라이드 기반으로". ① 발표 구성(F-20)은 실데이터
  세션이면 버튼 없이 자동 요청(페이지 수명당 1회 가드 — 실패 반복이 과금
  루프가 되지 않게, 샘플은 버튼 유지). ② 개요 이미지는 개념 그래프·판정으로
  노드를 만들고 좌표를 노드 수에서 계산(고정 좌표표는 샘플 9칸 전용이었다).
  ③ 펀치라인은 예상 질문의 모범답(answer_gist) 우선, 없으면 그래프 핵심
  문장. ④ 용어 카드는 그래프 요약(정의)+판정 상태+F-08 질문·골자. 재료가
  없으면 샘플로 위장하지 않고 빈 상태를 말한다.
- 검증: node --check, pytest 567 유지. 실데이터 육안 확인은 리허설에서.

### 남은 것

없음 — 감사에서 확인된 항목은 전부 수정됐다. 이후 버그는 새로 발견되는 대로
이 일지에 적는다.

### 운영 메모

- 브리지는 `DEMO_PORT=8799 ./demo/run_bridge_midm.sh` 로 실행 중, LoRA 예열
  완료(provider=lora). 서버 코드(bridge.py·session_store.py)를 고치면 반드시
  재시작 + 재예열해야 한다.
- **병행 세션 주의**: 같은 날 다른 Claude 세션이 UI 개편을 병행하며 캐시
  버전(?v=)을 qa63→qa54→qa55→qa56→qa73/74 로 자기 계보로 되돌리는 일이
  반복됐다. 과거에 서빙된 값으로 내려가면 옛 캐시를 든 브라우저가 낡은
  파일을 쓴다 — 버전은 **어느 계보에서든 한 번도 안 쓴 값으로만 올린다**
  (현재 qa75). 두 세션이 같은 파일을 만지면 미커밋 수정이 상대 커밋에 쓸려
  들어간다 — 한쪽을 멈추거나 담당 화면을 나누는 것을 권한다.
- 지난 QA 감사(11건)의 완결 기록은 [QA_FIXES.md](QA_FIXES.md) 에 있다.
