.venv/
__pycache__/
*.pyc
.env
.DS_Store
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
_adot_*
_stt_*
_service_*
_chunk*
_out.txt
_doc.html
demo/YEHS_demo/.git/

# logs / local dumps
*.log
fixtures/*.log
# LibreOffice PPTX→PDF 미리보기 캐시 (로컬 생성물)
fixtures/raw/*.preview.pdf
_adot_*/
_stt_*/
_service_*/
_chunk*/
_out.txt
_doc.html

# editor
.idea/
.vscode/
var/
__pycache__/
*.pyc

# Netlify 빌드가 chuckchuck/sdk 에서 복사해 만드는 사본 (netlify.toml 참고)
demo/YEHS_demo/sdk/

# 데모 입력 녹음 (11MB). 이력에 들어가면 되돌리기 어렵다 — PDF 만 넣는다
ppt/*.m4a
ppt/*.mp3
ppt/*.wav
ppt/*.webm

# 화면 확인용 스크린샷 (로컬 산출물)
screenshots/

# 단계 결과 디스크 캐시 (F-06·F-07). 로컬 산출물이라 이력에 넣지 않는다
fixtures/raw/stage_cache/

# 녹음 받아쓰기는 발표자 음성 전문이라 로컬에만 둔다 (2026-08-08 지시).
# 재사용은 fixtures/raw 에 그대로 있으면 되고, 저장소에 올릴 이유는 없다.
fixtures/raw/*.transcript.json
